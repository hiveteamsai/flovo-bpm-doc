# v0.33 — Tier 3 açık maddeler kapatıldı (Status kategori/grup=gerek yok · Çeviri güncelleme=manuel · idleTimeout=sınır yok+timezone kapsam-dışı · örnek adlandırma=izlenmez · swipe=ActionType.delete · action=string?) + Form List ayarları (reOrder/editOnlyOwnPosition profil-bazlı · parameterTransfer/propertyTransferParameters kaldırıldı→parentProperty) + ActionTransfer DTO klasörü/modeli + todo.md hijyeni (çözülenler 📦'ya)

> **Önceki:** [`v0-32.md`](./v0-32.md)
> **Kapsam:** **Tier 3 açık maddeleri** kapatıldı — **Status kategori/grup** (gerek yok; `code`/`definition` yeterli) · **Çeviri**
> (ortak kayıt güncelleme rutin değil → nadir ihtiyaçta **manuel**) · **`idleTimeoutMinute`** (alt/üst sınır **yok**; `0`=disable / `>0`=logout;
> **timezone kapsam-dışı**) · **örnek adlandırma** (izlenmez — örnekler sonra baştan) · **swipe item** = **`ActionType.delete`** (yeni değer) ·
> **`action` nesnesi şekli** = **`string?`** (aksiyon kodu; boş → `default`). **Form List ayarları:** `reOrder` + `editOnlyOwnPosition`
> (mapViewer) **profil-bazlı**, `parameterTransfer`/`propertyTransferParameters` **tamamen kaldırıldı** (ana↔alt akış → **`parentProperty`**).
> **ActionTransfer** artık **DTO** — `models/service-settings/dto/` klasörü açıldı (**genel kural: DB-olmayan modeller `dto/`'da**).
> **todo.md hijyeni:** çözülen `[x]` maddeler **📦 konsolide**ye taşındı (açık listelerde yalnız `[ ]`). **Bu dosya kapandı; sonraki sürüm `v0-34.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Bekleyen açık konular (`todo.md`):** **İki-katman sınırı** (değer atama & karşılaştırma → iş kuralı=frontend UX ↔
> adım=kalıcı/akış kararı; iş kuralı en son şekillenecek) · **Veri modeli — temsil ÇÖZÜLDÜ (v0.30, koleksiyon-tabanlı);**
> açık kalan: **soy ağacı (lineage)** + **çok-kayıt iterasyonu / loop / join** (item-dizisi fan-out → paralel dallanma) ·
> **Vekalet (proxy) sistemi** (görev-devri yerine kalıcı vekalet; `UserProxy` model + kapsam + iz/log + zincir kararları —
> ayrıntı sonra) · **O3** döngü koruması (derinlik/hop sınırı) · **O6** dış referans anahtarı
> `organizationId` vs `organizationCode` (Customer API) · ServiceTrigger **`timer` saat dilimi/DST** · **`async` kaskad
> kompozisyonu/derinlik sınırı** · **`triggerProcessStep → associatedInstance`** tetikleme tanımı · **D3** satır↔ekstre iş
> kuralı ↔ alt-süreç kalıcılık kararı · **`apiKeyId`** içeriği (Customer API) · **loglama modeli**
> (`research/settings-log/`; sistem logları açık) · **ortam (env) /
> servis template** · Form List tik & filtreler · motor iç mekaniği
> (orkestrasyon↔yürütme, Tier 1) · **adım ayar detayları** (Flovo AI · `settings` JSONB doğrulama · `triggerProcessStep`/
> `formRedirect` ayarları · `DynamicParameter.value` şekli) · **`ActionTransfer.user`** ile creatorUserId opsiyonel atama ·
> **`action` nesnesi şekli** (HTTP response tetikleme paketi) · **`idleTimeoutMinute`** alt/üst sınırı + Organization sonraki
> alanları · **`BusinessRuleCondition` ikili yuvalama** (iş kuralı fazında) · **`SchedulerJob` `...At` adlandırma** (altyapı-erteleme).

---

## 🆕 Eklenenler
- **`ActionType.delete` (yeni değer)** — aksiyon alınınca form **UI'dan kaldırılır**; **card görünümünde** bu tipteki aksiyon **swipe item**
  (kaydırmalı aksiyon) olarak render edilir. Swipe = ayrı `actionDisplayType` değil, **`delete` türüne** bağlı sunum. İşlendi:
  `models/enums/action-type.md` (tablo+Notlar) · `models/enums/index.md` · `organization-settings/action.md` ·
  `service-settings/process-step-action.md` §3.8.
- **DTO klasörü + `ActionTransfer` DTO modeli:** `models/service-settings/dto/` klasörü açıldı; **genel kural (proje geneli)** —
  **DB-modeli olmayanlar** (yalnız JSON olarak taşınan/saklanan paketler) ilgili kırılımın **`dto/`** klasöründe toplanır. İlk DTO:
  **`action-transfer.md`** (`parameters`/`changeList`/`action`; `ProcessStepInstance.processStepActionParameter`'da JSON string; formal
  model dosyası yoktu — §2 davranış dökümanından çıkarıldı). İşlendi: yeni `dto/action-transfer.md` + `dto/index.md` · `models/index.md`
  (DTO konvansiyonu notu) · `models/service-settings/index.md` (alt klasör) · çapraz-ref: `process-step.md` §3.5 · `process-step-instance.md` ·
  `todo.md` (ActionTransfer.user + `action` şekli maddeleri DTO'ya bağlandı).

## 🔧 Değişenler / kararlar
- **KARAR — Form List ayarları gözden geçir:** `reOrder` → **profil-bazlı** (`ProcessViewProfilePropertySetting`) · `editOnlyOwnPosition`
  (**mapViewer** alanı, Form List'te yanlış listeliymiş) → **profil-bazlı** · **`parameterTransfer`/`propertyTransferParameters`
  tamamen kaldırıldı** (tek bir "parametre transferi" mekanizmasıydı; ana↔alt değer akışı artık **`parentProperty`** salt-okunur
  yansımasıyla). **Not:** kaldırılan `propertyTransferParameters` **veri-kaynağı (§2.4 seçim alanları)** listesinden de düştü — dinamik
  veri-kaynağı parametre geçişi gerekirse iş kuralı **`fillDataSource`** ile yapılır. İşlendi: `service-settings/properties.md`
  §2.4/§3.12/§3.13 · `models/service-settings/property.md` (§1.4 satır kaldırıldı · mapViewer/formList · not) ·
  `models/service-settings/view-profile-property.md` (reOrder + mapViewer key) · `service-settings/view-profile.md` ·
  `models/processInstances/propertyValuesTemplates/form-list.md`.

## 🩹 Düzeltmeler
- **todo.md hijyeni — çözülen (`[x]`) maddeler açık listelerden çıkarıldı:** açık tier listelerinde (Tier 1/2/3 + "eklenen açık
  sorular") duran **11 çözülmüş madde** — Property value · İnsan-görev ailesi · Üst Form Kullanıcı · Çekirdek↔tipe-özel · Status
  kategori/grup · Çeviri · idleTimeout · Örnekler adlandırma · Swipe item · Customer API O6 · Form validasyon — **"📦 konsolide
  çözülmüş maddeler"** bölümüne (kısa özet + commitNote işaretçisi) taşındı. Artık açık listelerde yalnız `[ ]` kalıyor (todo'nun kendi
  kuralı). Açıklayıcı detay commitNotes + tasarım dökümanlarında.

## ❓ Yeni açık sorular
_(henüz yok)_

## ✅ Çözülen açık sorular
- **Status: kategori/grup (Tier 3)** → çözüldü: Status'a ayrı **kategori/grup boyutuna gerek yok**; raporlama/filtreleme için
  `code`/`definition` yeterli. İşlendi: `todo.md` (madde `[x]`) · `organization-settings/status.md` §4 · `models/organization-settings/status.md` Notlar.
- **Çeviri: ortak (null) kaydın sonradan güncellenmesi (Tier 3)** → çözüldü: **rutin akış değil**; override ayrı satır olduğundan
  organizasyon kayıtları **etkilenmez**, nadir gerçek ihtiyaçta **vaka-bazlı karar + manuel data güncellemesi** (otomatik kaskad/koruma
  yok). İşlendi: `todo.md` (madde `[x]`) · `organization-settings/translation.md` §5 · `models/organization-settings/translation.md` Notlar.
- **`idleTimeoutMinute` alt/üst sınır (Tier 3)** → çözüldü: **sınır yok** — `0`=disable, `>0`= organizasyonca belirlenen dakika → süre
  dolunca **logout** (tam yeniden giriş); sistemce dayatılan min/max yok. Ayrıca **timezone** ayrı açık konu olmaktan çıkarıldı
  (Organization'a timezone alanı **planlanmıyor**; timer DST maddesindeki timezone bağımlılığı da düşürüldü). Diğer Organization alanları
  (plan/abonelik, para birimi, bölge, güvenlik) = ihtiyaç anında eklenir. İşlendi: `todo.md` (idleTimeout `[x]` + timer maddesi) ·
  `organization-settings/organization.md` §2/§5 · `models/organization-settings/organization.md` Notlar.
- **Örnekler arası aksiyon kodu adlandırma tutarlılığı (Tier 3)** → izlenmez: örnek-düzeyi doc-hygiene açık soru olarak tutulmaz;
  **dökümanlar tamamlanınca örnekler baştan eksiksiz yeniden oluşturulacak**. İşlendi: `todo.md` (madde `[x]`) · `sampleProcess/index.md` (durum notu).
- **Aksiyonlarda swipe item (Tier 3)** → çözüldü: ayrı görünüm ayarı değil; `ActionType`'a **`delete`** tipi eklendi (aksiyon → form
  UI'dan kaldırılır; card görünümünde **swipe item**). `actionDisplayType` değişmedi. İşlendi: `todo.md` (madde `[x]`) + yukarıdaki 🆕 dosyalar.
- **Form List ayarları gözden geçir (Tier 2)** → çözüldü: `reOrder` + mapViewer `editOnlyOwnPosition` **profil-bazlı**;
  `parameterTransfer`/`propertyTransferParameters` **kaldırıldı** (→ `parentProperty`). İşlendi: yukarıdaki 🔧 kaydı + `todo.md`
  (📦 konsolide çözülmüşlere taşındı).
- **`action` nesnesinin şekli (Tier 3)** → çözüldü: `action` = **`string?`** (aksiyon **kodu**, obje değil) — **doluysa** adıma bağlı
  **aynı `code`'lu** aksiyon tetiklenir, **boş/null** → **`default`** aksiyon. İşlendi: `dto/action-transfer.md` §0/§1/§4 ·
  `service-settings/process-step-action.md` §2 (tablo + JSON iskeleti) · `todo.md` (📦'ya taşındı; `changeList` v0.30 notuyla birlikte).

---

*Commit başlığı: **v0.33**.*
