# v0.35 — İş kuralı motoru: davranış spesifikasyonu + fonksiyon-düzeyi işleyiş (business-rule-engine) + backend endpoint'leri + kodlanabilirlik doldurma (runtime modeli · golden fixture) + 6 motor kararı (JSONLogic · propertyType-karşılaştırma + containsAny/All · gömülü-JSONB koşul · nokta-yolu httpRequest · hibrit prepareEngine · readonly)

> **Önceki:** [`v0-34.md`](./v0-34.md)
> **Kapsam:** İş kuralı **motoru** doküman ailesi tamamlandı — **davranış spesifikasyonu** (`business-rule.md` §6–§10:
> tetikleme/koşul/aksiyon/değer/eşzamanlılık), **fonksiyon-düzeyi işleyiş** (`business-rule-engine.md`: çağrı grafiği +
> 28 fonksiyon spesifikasyonu + runtime modeli §0.1–0.3 + golden fixture §12), **backend endpoint'leri** (`business-rule-endpoints.md`:
> 6 durum). **Kodlanabilirlik** implementer-gözü analizle denetlendi, boşluklar dolduruldu ve **6 motor kararı** karara bağlandı
> (JSONLogic · propertyType-karşılaştırma + `containsAny`/`containsAll` · gömülü-JSONB koşul · nokta-yolu httpRequest · hibrit
> prepareEngine · readonly). **Bu dosya kapandı; sonraki sürüm `v0-36.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Bekleyen açık konular (`todo.md`):** **İki-katman sınırı** (değer atama & karşılaştırma → iş kuralı=frontend UX ↔
> adım=kalıcı/akış kararı) · **İş kuralı — açık detaylar** (model iskeleti v0.34'te kuruldu; kalan: `any`/`every` karşılaştırma ·
> `httpRequest` şeması · `shouldNotWorkInReadonlyMode` · `search` · `setStyle` style-şeması · `showMessage` bir-kez-göster ·
> ifade dili somut seçimi · davranış spesifikasyonu) · **Veri modeli — temsil ÇÖZÜLDÜ (v0.30, koleksiyon-tabanlı);** açık kalan:
> **soy ağacı (lineage)** + **çok-kayıt iterasyonu / loop / join** (item-dizisi fan-out → paralel dallanma) · **Vekalet (proxy)
> sistemi** (`UserProxy` model + kapsam + iz/log + zincir — ayrıntı sonra) · **O3** döngü koruması (derinlik/hop sınırı) · **O6**
> dış referans anahtarı `organizationId` vs `organizationCode` (Customer API) · ServiceTrigger **`timer` DST kenar durumları** ·
> **`async` kaskad kompozisyonu/derinlik sınırı** · **`triggerProcessStep → associatedInstance`** tetikleme tanımı · **D3** satır↔ekstre
> iş kuralı ↔ alt-süreç kalıcılık kararı · **`apiKeyId`** içeriği (Customer API) · **loglama modeli** (`research/settings-log/`;
> sistem logları açık) · **ortam (env) / servis template** · Form List tik & filtreler · motor iç mekaniği (orkestrasyon↔yürütme,
> Tier 1) · **adım ayar detayları** (Flovo AI · `settings` JSONB doğrulama · `triggerProcessStep`/`formRedirect` ayarları ·
> `DynamicParameter.value` şekli) · **`ActionTransfer.user`** ile creatorUserId opsiyonel atama · **`SchedulerJob` `...At`
> adlandırma** (altyapı-erteleme).
>
> **Not:** Login/register (kimlik & erişim) işleyiş dökümantasyonu için yol haritası → ana dizinde [`login-register-plan.md`](../login-register-plan.md)
> (§6 kararları verilince Faz 1'den yazım başlar).

---

## 🆕 Eklenenler
- **İş kuralı davranış spesifikasyonu** — `service-settings/business-rule.md` motor davranışıyla tamamlandı (🟡 TASLAK → 🟢):
  §6 **tetikleme & zincirleme** (runtime tipleri tablosu + genişletilmiş **tetikleme kapsamı** [koşul + aksiyon/ifade alanları] +
  ön-filtreler + cascade/`priority`/döngü) · §7 **koşul değerlendirme** (recursive AND/OR · `compareType` çözümleme · operatör
  tip-semantiği) · §8 **7 aksiyon davranışı** (koşul-TRUE/FALSE tablosu + `clearIfConditionNotTrue`/`notProvidedDeactive`/salt-okunur/
  satır-bazlı `withFormList`) · §9 **değer çözümleme** (ValueAssignType + async + çok-dilli) · §10 **eşzamanlılık & hata**. Açık
  davranış incelikleri (tip-semantiği detayı · `shouldNotWorkInReadonlyMode` uygulanışı · hata görünürlüğü) → `todo`. **0 kırık link.**
- **İş kuralı motoru — işleyiş & fonksiyon spesifikasyonu** (`service-settings/business-rule-engine.md`, YENİ): eski `WorkRuleService`
  (28 fonksiyon, 2103 satır) metod-metod incelenip **yeni-motor fonksiyon spesifikasyonu** olarak yazıldı — **çağrı grafiği** (üst→alt) +
  her fonksiyon için **imza · görev · tetiklenme · üretim/etki · çağırdığı fonksiyonlar**. Gruplar: giriş/orkestrasyon
  (`prepareEngine`/`executeRules`/`dispatchAction`) · tetikleme (`ruleAppliesTo`/`findTargetProperty`) · zincirleme & döngü ·
  7 aksiyon işleyici · koşul motoru (`evaluateConditionGroup`/`evaluateCondition`/`compareValues`/`resolveCompareValue`/`readPropertyValue`) ·
  değer çözümleyiciler (`resolveAssignValue`/`callHttpRequest`) · veri kaynağı (`fetchServiceInstances`/`buildDataSourceItems`/…) ·
  expression (`evaluateExpression` + fonksiyon kataloğu). Yeni kararlar (priority · genişletilmiş tetikleme · discriminated union ·
  httpRequest · çok-dilli ifade) yansıtıldı; açık noktalar `🟦` işaretiyle todo'ya bağlandı. `business-rule.md` §5 + `service-settings/index.md`'den atıf. **0 kırık link.**
- **İş kuralı motoru — backend endpoint'leri** (`service-settings/business-rule-endpoints.md`, YENİ): eski motorun yaptığı **tüm backend
  çağrıları** gerçek koddan envanterlenip **yeni-motor endpoint spesifikasyonuna** dönüştürüldü — 6 durum: §1 kural taşıma (gömülü) ·
  §2 `serviceInstances` (`POST /instances/search`) · §3 organizasyon verisi (`GET /organization/{resource}` — 10 kaynak) · §4 kullanıcı
  verisi (`GET /me/*`) · §5 `httpRequest` (dinamik, kullanıcı-tanımlı) · §6 ifade-destek uçları (kur/kullanıcı/çalışma-takvimi/grup;
  ağa giden ↔ ön-yüklenen cache ayrımı) · §7 kural yönetimi (REST CRUD). Her uç: method/path/amaç/istek/yanıt/tetikleyen. Yeni kararlar:
  **Keycloak Bearer + `organizationId` RLS** (eski accountId/Ocp-Apim yerine) · **Eba ucu kaldırıldı** (→httpRequest) · dataset→`instances/search`.
  `business-rule-engine.md` + `service-settings/index.md`'den atıf. **0 kırık link.**

## 🔧 Değişenler / kararlar
_(henüz yok)_

## 🩹 Düzeltmeler
- **Kodlanabilirlik gap doldurma** (implementer gözüyle analiz sonrası): `business-rule-engine.md`'ye **§0.1–0.3 runtime modeli**
  (`Property` runtime nesnesi · idempotency/değer-eşitliği · `DataSourceItem`/`ValidationItem`), **§7 httpRequest yanıt-çıkarım**
  şeması, **§12 golden fixture** (2 tam `BusinessRule` kaydı JSON'u — configuration+koşul+runtime birlikte) eklendi.
  Tutarsızlık düzeltmeleri: alan eşleşmesi **`propertyId`** (code ikincil) · `childPropertyId` yalnız Form List aksiyonlarında.
  DTO: `assign-value` httpRequest atfı (process-step §3.2 → engine §7) · `fill-data-source-organization`/`-user` **öğe eşlemesi**
  (`{value: code, display: definition, subText}`). Kalan **karar bloklayıcıları** (ifade dili · compareValues tip-semantiği ·
  koşul depolama · httpRequest alan-yolu · prepareEngine stratejisi · env/readonly) `todo` "kodlanabilirlik gap"a işlendi. **0 kırık link.**

## ❓ Yeni açık sorular
_(henüz yok)_

## ✅ Çözülen açık sorular
- **İş kuralı motoru — 6 karar bloklayıcısı karara bağlandı (v0.35):**
  **(1) İfade dili = JSONLogic** + yerleşik fonksiyon kataloğu (custom operator; veri-only → sandbox içsel; tam-frontend + Dart/JS portu;
  CEL Dart eksikliği nedeniyle elendi; async operatör sarmalayıcı). **(2) `compareValues` tip-semantiği = `propertyType`-güdümlü**
  (deterministik) + iki-taraf-fixedValue fallback + kanonik sayı + null tarih + **`containsAny`/`containsAll`** (`criterion-type`'a eklendi →
  eski `any`/`every` boşluğu kapandı). **(3) Koşul depolama = gömülü JSONB** (`BusinessRule.businessRuleConditions`; ayrı tablo/PK/FK yok →
  `business-rule-condition.md` ilişkisel alanları düştü). **(4) `httpRequest` çıkarım = basit nokta-yolu** (`responseParameter`/`valueField`/
  `displayField`). **(5) `prepareEngine` = hibrit ön-yükleme** (org/user cache-sync + serviceInstances lazy). **(6) `shouldNotWorkInReadonlyMode`
  = motor uygular** (env `environmentRestriction` ortam modeline ertelendi). İşlendi: `business-rule-engine.md` (§3/§6/§7/§9/§11 + golden-fixture notu) ·
  `business-rule.md` (§2/§3.1/§6.3/§7.3) · `business-rule-condition.md` + `models/business-rule.md` (gömülü) · `criterion-type.md` + `enums/index.md`
  (`containsAny`/`containsAll`) · `business-rule-endpoints.md` (§3/§9) · `todo.md` (kararlar). **0 kırık link.**

---

*Commit başlığı: **v0.35**.*
