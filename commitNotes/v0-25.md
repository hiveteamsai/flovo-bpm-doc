# v0.25 — Baştan analiz + düzeltme turu: Y1/Y2 (subProcessStart runtime & Processing opsiyonel) + ServiceTrigger netleştirmeleri + örnek/isim düzeltmeleri + DTO-provenans compare'a taşındı

> **Önceki:** [`v0-24.md`](./v0-24.md)
> **Kapsam:** Projenin **baştan detaylı analizi** (paralel ajanlarla) → bulunan tutarsızlık/mantık sorunları geçici bir
> `issue.md` takip dosyasında toplanıp **madde madde** çözüldü (Y1–Y2 · O1–O12 · D1–D17), ardından **düzeltme-sonrası
> çapraz-analiz** (regresyon) yapıldı ve **DTO-provenans notları** `research/compare/`'a taşındı. `issue.md` iş bitince silindi.
> Kararlar N1–N5 notlarıyla birlikte ilgili tasarım dokümanlarına işlendi. **Bu dosya kapandı; sonraki sürüm `v0-26.md`.**
>
> **Bekleyen açık konular (`todo.md`):** **O3** döngü koruması (motor-seviyesi güvenlik ağı) · **O6** dış referans anahtarı
> `organizationId`/`organizationCode` (Customer API detaylanınca) · ServiceTrigger `timer` tz/DST · `async` kaskad derinliği ·
> `triggerProcessStep → associatedInstance` alt süreç/aksiyon tetikleme tanımı · Property value depolama · Customer API ·
> loglama · env/template · Üst Form Kullanıcı kenar durumları · Tier 1/2 kalanlar.

---

## 🆕 Eklenenler
- **Motor §5.3 — "Olay/otomatik tetikleme (ServiceTrigger)"** bölümü (`flovo-bpm-engine.md`): associate (ilişki değişimi) + timer + async.
- **ServiceTrigger `targetServiceId` invariant'ı** (`== targetPropertyId Form List→childServiceId / Combobox→associatedServiceId`) + **`async` bekleme yeri** (tetiklendiği yerde — AssociatedInstance yazımını yapan işlem).
- **`subProcessStart` tetikleme kaynağına 3. yol:** ServiceTrigger (associate) — `process-step.md` §3.20 (prose) + §3.16 (models).
- **`ProcessInstance` kararı:** "`instance` alanı **eklenmez** + bağ **dolaylı** + instance başına **1 ana + N alt** süreç" kanonik olarak `process-instance.md`'ye yazıldı.
- **compare/new-vs-current-names.md §11:** per-model **Kaynak DTO tablosu** (14 model → eski DTO + araştırma dosyası) + Position/Staff ad eşlemesi (provenans buraya taşındı).
- **expenseAndCreditCard:** `subProcessEnd` adım satırı (Tutar), `expenseIds` `list.add`/`list.remove` adımı, D3–D7 netleştirme notları (iş kuralı reaktif · totalAmount asimetri gerekçesi · fill `used==false` · İK↔alt-süreç ikisi de gerekli).

## 🔧 Değişenler / kararlar
- **Y1 — subProcessStart runtime temsili:** alt süreç = **yeni `ProcessInstance`** (yeni **Instance değil**); `parentProcessInstanceId` = **hedef/host instance'ın ana süreci** (tetikleyen değil); instance'a bağ **dolaylı** (`Instance.processInstanceId` zinciri); `ProcessInstance`'a `instance` alanı **eklenmez**. → `service-trigger.md`, `process-instance.md`, `process-step.md` §3.20/§4, `process-step-instance.md`, `todo.md`.
- **Y2 — Processing:** otomatik ilerleme **opsiyonel** — `default` kodlu `actionType=autoAction` **varsa** ilerler, **yoksa bekler**. → `flovo-bpm-engine.md` §4.3/§6.1, `process-step.md` (prose §3.18 + özet + models §3.13), `process-step-type.md`, `process-step-action.md` §3.7, `sampleProcess/integration`, `todo.md`.
- **Motor §6.3:** "form döndüren **dört adım**" → **"beş adım"** (Üst Form Kullanıcı eklendi) + §0 human-task özeti.
- **Kullanıcı Grubu FK:** `organizationUserGroupId(s)` → **`userGroupId` / `userGroupIds`** (tüm design + enum + todo + compare).
- **`Instance` yalnızca Instance Creator ile oluşur** — `service.md` + `form-type.md` hizalandı.
- **`default`** bir **ProcessStepType değeri değildir** (aksiyon-yönlendirme kodu) — `process-step-type.md` netleşti.
- **expenseAndCreditCard yeniden adlandırma:** `creditCardStateLine`→`matchedStatementLine`, `crediCardStatementLineDoldurma`→`matchedStatementLineDoldurma`, `remaning`→`remaining`, `enable`→`enabled`, "Form Creater"→"Form Creator"; alt-süreç sonu → **Alt Süreç Bitişi (`subProcessEnd`)**; isim setleri ayrıldı (Masraf: "Forma Ekle/Formdan Çıkart Başlangıcı"; Ekstre Satırı: "Masraf İlişkilendirme/İlişki Kaldırma Başlangıcı"); `expenseIds` = `combobox` (`isMultiSelect=true`, `isAssociatedCombobox=false`).
- **DTO-provenans → compare'a taşındı** (14 org-model başlığı + processInstances "Eski karşılığı" + models inline "Kaynak DTO") — CLAUDE.md kuralı 4.

## 🩹 Düzeltmeler
- **Eski-app sızıntısı temizlendi:** `authorizationLevel` (permissions §0 · organization ×2 · user · models/index · keycloak); `withForm` → yalnız compare; `managerChain`/`managerByTitle` aktif-anlatımları (user ×3 · profession · org/index · models/index · sampleProcess/index) → yalnız "kaldırıldı" notu.
- **Küçük düzeltmeler:** UserGroup FK üç-ad tutarsızlığı · `targetStarterProcessStepId` invariant notu detaylandı · çıplak "(davranış → §3.X)" → tam-yol (16 atıf) · `usedAmount`/`totalAmount`/`used` iş-kuralı netleştirmeleri · `mergeParameter=true` işaretleri (createPdf/Async) · scanBarcode `redir`→`redirect` & "(Function)"→HTTP Request · `properties.md` → view-profile-property pointer · expense **2 Instance Creator** etiket birleştirme · "Tutar Bitişi" hizalama · `form-type.md`/`process-step.md:77` Processing koşullu hizası.
- **Doğrulama:** düzeltme-sonrası çapraz-analiz (4 paralel ajan + mekanik tarama) → **kritik regresyon yok**; 532 iç-link kopuksuz; `organizationUserGroupId`/eski-ad/"tetikleyen ana süreç"/"dört adım"/`ProcessInstance.instance` sızması yok.

## ❓ Yeni açık sorular
- **`triggerProcessStep` → ilişkili instance (associatedInstance)** alt süreç/aksiyon tetikleme tanım-gapleri → `todo.md` ServiceTrigger açık madde (4).

## ✅ Çözülen açık sorular
- **Alt süreç runtime temsili** — `parentProcessInstanceId` = host instance'ın anası; `ProcessInstance`'a instance alanı eklenmez; bağ dolaylı.
- **Processing otomatik-ilerleme opsiyonelliği** (`default autoAction`).
- **ServiceTrigger:** `targetServiceId` invariant · `async` bekleme yeri · `subProcessStart` tetikleme kataloğu (3 yol) · motor §5.3.
- **DTO-provenans yerleşimi** — `research/compare/`'a taşındı (kural 4).
- **Kullanıcı Grubu FK adı** (`userGroupId`/`userGroupIds`) · **Instance yalnız Instance Creator ile oluşur** · **`default` ProcessStepType değil**.

---

*Commit başlığı: **v0.25**.*
