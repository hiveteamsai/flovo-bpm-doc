# v0.40 — Motor Runtime Mimarisi (`engine-runtime.md`) — senkron yürütme döngüsü → event-driven state machine (Go worker + NATS + Postgres event-sourcing) + `ProcessExecutionState`

> **Önceki:** [`v0-39.md`](./v0-39.md)
> **Kapsam:** **Motor runtime mimarisi** (`engine-runtime.md`, YENİ top-level) yazıldı — senkron §4.4 döngüsü → **event-driven,
> kalıcı, kaldığı yerden devam eden** state machine (Go stateless worker + NATS JetStream + Postgres `workflow_events` Partial
> Event Sourcing): yürütme durumu (`new/running/waiting/failed/done`) · **suspend/resume** · **at-most-once** idempotency ·
> lider-seçimli scheduler · **paralel = MVP tek aktif kol** (fork/join ertelendi). `ProcessInstance.executionState` alanı +
> `ProcessExecutionState` enum eklendi; `flovo-bpm-engine.md` §2.2/§8/§4.5 dolduruldu.
> **1333 iç linkten 0 kırık. Bu dosya kapandı; sonraki sürüm `v0-41.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Kilometre taşı (v0.36–v0.39):** Servis + org ayar **modelleri** TASLAK'tan çıkarıldı (v0.36); **tipe-özel `settings` JSON
> Schema'ları** property (18) + process-step (22) ile tamamlandı (v0.37–v0.38); **tasarım-zamanı ayar CRUD API yüzeyi** yazıldı
> (v0.39, `settings-api.md`). Böylece ayarlar fazının **model + davranış + settings-şema + API yüzeyi** iskeleti hazır.
>
> **Sıradaki adaylar:** **(a)** Settings API **derinleştirme** — Service+Property+ProcessStep için tam request/response şemaları ·
> **(b)** Settings API **açık kararları** (draft/publish + versiyonlama · yetki granülaritesi · ortak hata sözleşmesi) ·
> **(c)** **Motor runtime mimarisi** (§2.2 orkestrasyon↔yürütme · §8 kalıcılık/durum · §4.5 paralel) — motoru çalışır kılan asıl faz.
>
> **Bekleyen açık konular (`todo.md`):** **Settings API açık noktaları** (§5–§9) · **Property/Process step `settings` açık alan
> kararları** · **İki-katman sınırı** · **İş kuralı — kalan detaylar** · **Motor runtime mimarisi** · **Veri modeli** (lineage +
> iterasyon) · **Vekalet (proxy)** · **O3/O6** · ServiceTrigger DST/async · **D3** · `apiKeyId` · **loglama modeli** · **ortam (env)** ·
> Form List tik & filtreler · adım ayar detayları · `ActionTransfer.user` · `SchedulerJob` `...At`.
>
> **Not:** Login/register (kimlik & erişim) işleyiş dökümantasyonu için yol haritası → ana dizinde [`login-register-plan.md`](../login-register-plan.md)
> (§6 kararları verilince Faz 1'den yazım başlar).

---

## 🆕 Eklenenler
- **Motor Runtime Mimarisi** (`engine-runtime.md`, YENİ, top-level): senkron §4.4 yürütme döngüsünün **event-driven state
  machine**'e çevrimi (Go stateless worker + NATS JetStream + Postgres `workflow_events` Partial Event Sourcing). §0 reframe ·
  §1 **yürütme durumu state machine** (`new/running/waiting/failed/done`) · §2 bileşenler (worker↔orkestratör↔scheduler) ·
  §3 NATS BPM subject'leri · §4 yürütme akışı (başlat/otomatik-adım/**suspend**/**resume**/bitiş) · §5 idempotency (Nats-Msg-Id +
  event version) · §6 hata/retry · §7 timer/uyandırma (lider-seçimli scheduler) · §8 **paralel KARAR** · §9 kalıcılık/yaşam-döngüsü ·
  §10 ölçekleme/çok-kiracılık. **1333 linkten 0 kırık.**
- **`ProcessExecutionState` enum** (`models/enums/process-execution-state.md`) + `ProcessInstance.executionState` alanı — motor
  yürütme durumu (`Instance.statusId` iş-durumundan ayrı; `workflow_events`'ten türetilen projeksiyon).

## 🔧 Değişenler / kararlar
- **`flovo-bpm-engine.md` §2.2/§8/§4.5 dolduruldu** → engine-runtime.md'ye bağlandı (placeholder "founder kararı bekliyor" notları
  ÇÖZÜLDÜ'ye çevrildi). Top-level `index.md` + `enums/index.md` güncellendi.

## 🩹 Düzeltmeler
_(henüz yok)_

## ❓ Yeni açık sorular
- **Motor runtime kalan açık noktaları** (todo Tier 1'e işlendi): `workflow_events`/`workflow_projection` model dosyaları ·
  scheduler lider-seçim mekanizması · retry politika + global hata yakalayıcı + compensation · fork/join (ertelendi) ·
  `workflow_events` pruning/KVKK · optimistic-concurrency çakışma UX'i.

## ✅ Çözülen açık sorular
- **Motor-içi orkestrasyon↔yürütme mimarisi — ÇÖZÜLDÜ (v0.40):** senkron döngü → dağıtık event-driven state machine;
  suspend/resume ile uzun-soluklu süreçler kaynak tutmadan bekler; at-most-once (idempotency); **paralel = MVP tek aktif kol**
  (eşzamanlılık ayrı ProcessInstance'larla; fork/join ertelendi). → engine-runtime.md.

---

*Commit başlığı: **v0.40**.*
