# v0.39 — Flovo Settings API (tasarım-zamanı ayar CRUD yüzeyi) — `settings` doğrulaması tipe-özel JSON Schema'lara bağlandı

> **Önceki:** [`v0-38.md`](./v0-38.md)
> **Kapsam:** **Tasarım-zamanı ayar CRUD API'si** (`settings-api.md`, YENİ top-level) yazıldı — **üç API sınırı** ayrıldı
> (Settings [design-time] ↔ Customer [runtime veri] ↔ business-rule-endpoints [motor+kural]); servis-ayarı + org-ayarı kaynakları
> için CRUD yüzeyi; **`settings` doğrulama kapısı** tipe-özel JSON Schema'lara (property-settings/process-step-settings) bağlandı;
> ortak sözleşme (Keycloak+RLS · Protobuf/gRPC/REST · `code` immutable · soft-delete). Draft/publish · versiyonlama · ortak hata
> sözleşmesi · toplu senkron · loglama · yetki granülaritesi **açık bırakıldı** (§7–§9 → todo).
> **1311 iç linkten 0 kırık. Bu dosya kapandı; sonraki sürüm `v0-40.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Kilometre taşı (v0.36–v0.38):** Servis + org ayar **modelleri** TASLAK'tan çıkarıldı (v0.36); **tipe-özel `settings` JSON
> Schema'ları** property (18, v0.37) + process-step (22, v0.38) ile **TAMAMLANDI**. **Sıradaki:** **Ayar-yazma (design-time
> CRUD) API yüzeyi** — service/property/process-step/view-profile/business-rule tasarım-zamanı CRUD uçları (backend'i başlatan
> ikinci ön-koşul; `business-rule-endpoints §7` runtime tarafıydı, bu tasarım-zamanı).
>
> **Bekleyen açık konular (`todo.md`):** **Ayar-yazma (design-time CRUD) API yüzeyi** (aktif konu) · **Property/Process step
> `settings` açık alan kararları** (flowInfoValue/userInfoValue enum · Form List red-akışı · comparison değer-kaynağı/nested grup ·
> trigger/formRedirect şeması · flovoAi/instanceCreator) · **İki-katman sınırı** · **İş kuralı — kalan detaylar** (JSONLogic katalog
> imzaları · environmentRestriction · hata görünürlüğü · cascade döngü/derinlik · search · setStyle şeması · showMessage · httpRequest
> JSONPath) · **Motor runtime mimarisi** (§2.2 orkestrasyon↔yürütme · §8 kalıcılık/durum · §4.5 paralel) · **Veri modeli** açık:
> lineage + çok-kayıt iterasyon/loop/join · **Vekalet (proxy)** · **O3** döngü · **O6** dış referans anahtarı · ServiceTrigger
> `timer` DST · `async` kaskad · `triggerProcessStep → associatedInstance` · **D3** kalıcılık · `apiKeyId` · **loglama modeli** ·
> **ortam (env)** · Form List tik & filtreler · adım ayar detayları · `ActionTransfer.user` · `SchedulerJob` `...At`.
>
> **Not:** Login/register (kimlik & erişim) işleyiş dökümantasyonu için yol haritası → ana dizinde [`login-register-plan.md`](../login-register-plan.md)
> (§6 kararları verilince Faz 1'den yazım başlar).

---

## 🆕 Eklenenler
- **Flovo Settings API — tasarım-zamanı ayar CRUD'u** (`settings-api.md`, YENİ, top-level): design-time config CRUD yüzeyi —
  §1 ortak sözleşme (Keycloak Bearer + organizationId RLS · Protobuf→gRPC/REST · `code` immutable · soft-delete) · §2 kaynak
  hiyerarşisi · §3 servis-ayarı CRUD (solution/service/property[+item]/process-step[+action]/view-profile/business-rule/service-trigger) ·
  §4 org-ayarı CRUD (action/status/style/translation/user-group/referans veriler) · **§5 `settings` doğrulama kapısı** (tipe-özel
  JSON Schema — property-settings/process-step-settings'e bağlandı) + referans bütünlüğü/silme koruması · §6 toplu işlemler
  (servis tanımı export/import · toplu senkron) · §7 draft/publish+versiyonlama (açık) · §8 loglama (açık) · §9 açık noktalar.
  Üç API sınırı netleştirildi (Settings API [design-time] ↔ Customer API [runtime veri] ↔ business-rule-endpoints [motor+kural CRUD]).
  Top-level `index.md`'e eklendi. **1311 linkten 0 kırık.**

## 🔧 Değişenler / kararlar
_(henüz yok)_

## 🩹 Düzeltmeler
_(henüz yok)_

## ❓ Yeni açık sorular
_(henüz yok)_

## ✅ Çözülen açık sorular
_(henüz yok)_

---

*Commit başlığı: **v0.39**.*
