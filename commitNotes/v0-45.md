# v0.45 — `parentProperty` kararları (ilişki-anı kopya/temizleme · birincil üst · `parentPropertyId`/`refPropertyId` semantiği · faz kapsamı; `relatedPropertyIds` kaldırıldı) + Motor runtime anlatım/şema HTML'leri (`research/engine-runtime/`)

> **Önceki:** [`v0-44.md`](./v0-44.md)
> **Kapsam:** **(1)** `parentProperty` — dört karar tek sürümde: `snapshot`/`materialized` kopyası **ilişki kurulduğu anda** (`AssociatedInstance` insert) alınır,
> bağ kalkınca **`null`** (yeni `reflection-propagation.md` §3a) · aynı bağlayan alanda **birincil üst = en erken bağ** (§3.22 ile aynı) + Designer'da yalnız
> Form List / tek-seçimli Combobox · `parentPropertyId` = **bağlayan alan**, `refPropertyId` = **üst servisteki yansıtılan alan**, `relatedPropertyIds`
> **kaldırıldı** (eski DataGrid kolon listesiydi) · faz: `snapshot`/`live` Motor Faz 1, `materialized` **F1.A.4** ile (§10). Örnek süreç
> (`expenseAndCreditCard`) alan bazında mod aldı, dört "(açık)" tip teyidi kapandı; `todo.md` log genişletildi (20 dosya). **(2)** `research/engine-runtime/`
> — v0.44 runtime setinin özet+detay HTML anlatımı ve metot-düzeyi mimari şeması (📝 öneri; R18/Q23 plana işlenmedi, onay bekliyor).
> **Bu dosya kapandı; sonraki sürüm `v0-46.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
> **Geçici istisna (v0.44):** Çalışma-zamanı mimarisi konusunun kararları/açık soruları inceleme süresince
> [`../bpmEngine/architectures/engine-runtime/engine-runtime-plan.md`](../bpmEngine/architectures/engine-runtime/engine-runtime-plan.md)'de (R1–R17 · Q1–Q22); kesinleşince `todo.md`'ye taşınır.
>
> **Kilometre taşı (v0.36–v0.44):** Ayarlar fazı (model + davranış + tipe-özel settings + Settings API yüzeyi) · Motor runtime mimarisi
> (`engine-runtime.md`) · iki fazlı motor geliştirme planı (`bpm-engine-build-plan.md`) · pilot tutarlılık (v0.41-1/v0.42) · **v0.43:** İki-katman
> sınırı (Tier 0) ÇÖZÜLDÜ · **v0.44:** Çalışma-zamanı mimarisi eksik dokümanları **TASLAK set** (WorkflowEvent/Projection/Timer modelleri ·
> hata/dayanıklılık · zamanlayıcı/uyandırma · saklama/KVKK · `engine-runtime-plan.md`) — **kullanıcı incelemesi bekliyor**.
>
> **Sıradaki adaylar:** **(a)** **v0.44 runtime taslaklarının incelemesi** → geri bildirim (`R#: kabul` / `Q#: …`) → dokümanlara işleme + plan §5 review log →
> kesinleşince **`todo.md` güncellemesi** (Tier 1 "Çalışma-zamanı mimarisi" + ServiceTrigger DST/async + Hata yönetimi maddeleri) + `bpm-engine-build-plan.md`
> §2.0 panosu/F1.A–C `[x]` · **(b)** onaya bağlı model alanları: `ProcessStep.retryPolicy` (Q7) · `Service.onFailProcessStepId` (Q10, post-MVP) ·
> `Organization.retentionPolicy` (Q18) · permissions `processAdminUserGroupId` (Q12) · HTTP Request `idempotencyKeyHeader` (Q21) · **(c)** İki-katman türevi —
> `comparison.md` şemasını `BusinessRuleConditionCompareValue`'ya hizala (Tier 3) + çapraz-katman ifade/operatör kataloğu (Tier 2) · **(d)** Settings API
> request/response derinleştirme · **(e)** Grup D (adım-içi yürütme spec'leri) — runtime konusu kapanınca.
>
> **Bekleyen açık konular (`todo.md`):** motor runtime kalan (**inceleme bekliyor** — plan Q1–Q22) · Settings API kalan (hata sözleşmesi · ortamlar-arası kopya ·
> toplu senkron · loglama · yetki granülaritesi) · Ortam (env) modeli · İş kuralı kalan detaylar (ifade katalog kapsamı — çapraz-katman) · comparison şema hizalama ·
> Property/Process step `settings` açık alan kararları · Vekalet (proxy) · O3/O6 · apiKeyId · loglama modeli (sistem logları) · adım ayar detayları.
>
> **Not:** Ana dizin plan/durum dosyaları → [`bpm-engine-build-plan.md`](../bpm-engine-build-plan.md) (motor geliştirme) ·
> [`login-register-plan.md`](../login-register-plan.md) (kimlik & erişim) · [`bpmEngine/implementation-status.md`](../bpmEngine/implementation-status.md) (tasarım ↔ inşa/dağıtım).

---

## 🆕 Eklenenler
- **`bpmEngine/research/engine-runtime/`** (yeni klasör) — v0.44 çalışma-zamanı mimarisi taslak setinin **tek sayfalık özet + detay HTML anlatımı**:
  [`motor-runtime-v0-44-ozet-detay.html`](../bpmEngine/research/engine-runtime/motor-runtime-v0-44-ozet-detay.html) (özet: sorun/tez · üç tablo · aktörler · altı hareket · garantiler ·
  durum makinesi · sözlük · masraf-formu olay defteri örneği; detay: durum · WorkflowEvent · iki-TX turu · idempotency/409 sözleşmesi · Projection · Timer/Scheduler · hata · saklama/KVKK;
  inceleme: R1–R17 · Q1–Q22 · P1–P10 · doküman haritası) + [`index.md`](../bpmEngine/research/engine-runtime/index.md) (rol · kaynak dokümanlar · neden research).
  Tarayıcıda tek başına açılır (Google Fonts + mermaid cdnjs). **Anlatım/sunum çıktısıdır** — bağlayıcı metin `engine-runtime*.md` + modeller; set hâlâ onay bekliyor.
  `research/index.md`'ye alt-klasör satırı eklendi. Çevrimiçi kopya (Claude artifact) indeks dosyasında linkli.
- **`bpmEngine/research/engine-runtime/motor-mimari-semasi-v0-45.html`** — motorun **metot-düzeyi mimari şeması (📝 ÖNERİ)**: kuş bakışı bileşen diyagramı ·
  tablolar (yazan metotlar) · 3 akış diyagramı (kullanıcı aksiyonu inline zincir · worker işi/in-doubt/hata dalları · scheduler claim) · numaralı **çağrı ağacı**
  (düğüm başına I/U/D/R tablo etiketleri + TX sınırları) · **metot × tablo yazma matrisi** · adım yürütücüleri · olay → dispatch → NATS eşlemesi.
  Metot ayrıştırması dokümanlarda henüz yok (öneri); **R18 inline devam** (senkron ilerletme penceresi; dispatch tüketimi sonraki `BeginStep` TX'inde `publishedAt`) ve
  **Q23** (bütçe · inline adım tipleri · 202 gövdesi · `path[]`) bu şemada önerildi — plana işlenmesi kullanıcı kararı bekliyor. İndeks + plan §0 + `research/index.md` güncellendi.

## 🔧 Değişenler / kararlar
- `research/engine-runtime/motor-runtime-v0-44-ozet-detay.html` — **"Hız & performans değerlendirmesi"** bölümü eklendi (2026-09-04): kullanıcı-aksiyonu kritik yol maliyet dökümü
  (yerel/Azure PG, ≈10–50 ms) · gecikmenin gerçek kaynakları · tasarımın koruyan yanları · risk→önlem tablosu (PgBouncer, tasarım-tablosu cache, UUIDv7 …) ·
  benchmark hedefi önerisi (**aksiyon → response p95**: saf-hesap < 100 ms; dış çağrılı = dış çağrı + 30 ms). Değerlendirme **R18 inline-devam** önerisine dayalıdır (aday — plana işlenmedi).
- **`parentProperty` kopyalama anı — KARAR (2026-09-07):** `snapshot`/`materialized` kopyası **instance oluşturma anında değil, ilişki kurulduğu anda**
  (`AssociatedInstance` insert) üstten alınır; bağ **kaldırılınca `null`**'a çekilir; Combobox'ta seçim değişimi = temizle + yeni üstten kopyala; `live`'da işlem yok.
  Tespit ServiceTrigger ile aynı çekirdek DB katmanında, yazım ortak kapıdan (aynı TX + outbox). Gerekçe: child çoğu zaman bağdan önce var olur (Form List'te bile
  önce instance, sonra bağ; "var olandan ekle"); oluşturma-anı kopyası boş kalırdı. İşlenen dosyalar: `reflection-propagation.md` (**yeni §3a** + §6 iki satır) ·
  `enums/reflection-mode.md` (amaç + snapshot/materialized satırları + not) · `propertyValuesTemplates/parent-property.md` §1 · `properties.md` §3.15 ·
  `property.md` §1.5 · `jsonTemplateModels/property-settings/parent-property.md` §2/§4 · `associated-instance.md` (yeni yazım-yolu tüketicisi notu) ·
  `flovo-bpm-engine.md` yazma yolu adım 2 · `sampleProcess/expenseAndCreditCard/scenarios.md` (temel davranış + Sen-2/3/4/5) · `expense.md` notlar · `todo.md` (log + Tier 2).
- **`parentProperty` kalan üç nokta — KARAR (2026-09-07, aynı oturum):** **(a) birden fazla üst instance:** aynı bağlayan alanda **birincil üst = en erken bağ**
  (en küçük `AssociatedInstance.id`; §3.22 "ilk tespit edilen" ile aynı, artık deterministik) — insert yalnız ilk bağda kopyalar, delete'te birincil düşerse kalan
  en erken bağdan yeniden kopyalar, §4 yayılımı yalnız birincil üstten; **Designer kısıtı:** bağlayan alan yalnız Form List / tek-seçimli ilişkili Combobox
  (çok-seçimli reddedilir). **(b) FK semantiği:** `parentPropertyId` = **bağlayan alan** (üst servis buradan türetilir), `refPropertyId` = **üst servisteki
  yansıtılan alan** (üst servise ait olmalı — `SettingsValidator`); **`relatedPropertyIds` kaldırıldı** (teyit: eski uygulamada **DataGrid alt-alan/kolon listesi**ydi —
  `relatedPropertyDtos` ile çift, yalnız `DataGridPropertySettings`'te düzenlenir; DataGrid yeni tasarımda yok, `keyValueList`/`groupByTaxReceipt` alt-alanları sabit şablonla
  tanımlı → dinamik alt-alan listesine ihtiyaç yok; `parentProperty` ile ilgisi yoktu). **(c) faz:** `snapshot`/`live` Motor **Faz 1 çekirdek**; `materialized` **F1.A.4** (outbox/projector/NATS) ile — öncesinde
  Designer reddeder; örnek süreçte alan bazında mod: Masraf.`expenseFormId` `snapshot` · Ekstre Satırı.`creditCardStatementId` `snapshot` ·
  Masraf.`creditCardStatementId` **`materialized`+`async`** (F1.A.4 öncesi `live`). Reddedilen: Form List'e tekil-üst DB kısıtı (süreç tasarımı zaten
  önlüyor, kısıt "var olandan ekle" akışlarını sertleştirirdi) · `relatedPropertyIds`'i tanımlayıp tutmak (tek amacı DataGrid kolonlarıydı; DataGrid kaldırıldı). İşlenen dosyalar: `reflection-propagation.md`
  (§2 satır · §3a yazım satırı + iki yeni madde · §4 d-adımı birincil-üst guard'ı · **yeni §10 faz kapsamı**) · `reflection-mode.md` (faz notu) · `property.md` §1.5
  (iki FK satırı yeniden yazıldı, `relatedPropertyIds` satırı silindi; §2 özet) · `properties.md` §2.5/§3.15 · `jsonTemplateModels/property-settings/parent-property.md`
  §2/§4/§5 · `associated-instance.md` (birincil üst) · `process-step.md` §3.22 (aynı kural atfı) · `bpm-engine-build-plan.md` F1.A.4 · `research/compare/new-vs-current.md`
  §4 ➖ + footer · `sampleProcess/expenseAndCreditCard/expense.md` + `creditCardStatementLine.md` (alan bazında `reflectionMode`, "(açık)" PropertyType teyitleri kapatıldı) ·
  `scenarios.md` A4 · `todo.md` (Tier 2 maddesi kaldırıldı → log genişletildi).

## 🩹 Düzeltmeler
_(henüz yok)_

## ❓ Yeni açık sorular
- **(aday — plana henüz işlenmedi)** Kullanıcı aksiyonu response'u ↔ event-driven zincir boşluğu: engine-runtime §4.4 "dispatch `step_ready`" der, §6.3 "form response'ta döner" der, arayı yazmaz.
  Öneri **R18 inline devam** + **Q23** parametreleri (→ `research/engine-runtime/motor-mimari-semasi-v0-45.html` §Notlar). Kabul edilirse: `engine-runtime.md` §4.4/§5.1 ·
  `workflow-event.md` §3.5 (`publishedAt` = yayınlandı **veya** inline tüketildi) · `flovo-customer-api.md` aksiyon yanıtı (200/202 gövdesi) · `engine-runtime-plan.md` R18/Q23.

## ✅ Çözülen açık sorular
- **`parentProperty` ilk dolum / temizleme zamanı** (bu oturumda tespit edildi: `reflection-mode.md` "oluşturma anında" ↔ Senaryo 2/3 "ilişki oluşturmadan sonra"
  kuruluyor → kopya boş kalırdı) → **ilişki kurulunca kopyala, bağ kalkınca temizle** (kullanıcı kararı, 2026-09-07). → `todo.md` log · `reflection-propagation.md` §3a.
- **`parentProperty` — birden fazla üst instance** → **birincil üst = en erken bağ** (§3.22 ile aynı deterministik kural) + Designer'da yalnız Form List / tek-seçimli Combobox. → §3a.
- **`parentProperty` — FK semantiği + `relatedPropertyIds`** → `parentPropertyId` = bağlayan alan · `refPropertyId` = üst servisteki yansıtılan alan · `relatedPropertyIds` **kaldırıldı**. → `property.md` §1.5.
- **`parentProperty` — pilot/faz `reflectionMode`** → `snapshot`/`live` Faz 1; `materialized` F1.A.4 ile; örnek süreç alanlarına mod atandı. → `reflection-propagation.md` §10.

---

*Commit başlığı (atılınca): **v0.45**.*
