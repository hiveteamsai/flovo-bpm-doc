# v0.29 — Tüm-proje baştan tutarlılık analizi (5-alan audit) + isim standardizasyonu + `process-step-action` alan tamamlama + `InstanceValue` PK + `reflectionMode` davranış-yayılımı

> **Önceki:** [`v0-28.md`](./v0-28.md)
> **Kapsam:** **Tüm proje** 5 paralel okuyucuyla (models · service-settings · organization-settings · kök+todo · sampleProcess+compare)
> baştan **tutarlılık analizinden** geçirildi; bulgular kullanıcı kararlarıyla uygulandı. **İsim standardizasyonu:** geçmiş alanları
> **`showInHistory`** (öğe geçmişte görünür mü) **+ `showHistory`** (aksiyon tamamlanınca tarihçeyi otomatik göster) = **iki ayrı alan**
> (Action+ProcessStepAction); adım `hideInHistory`→`showInHistory` (pozitif) · `fromCalculate`→**`fromCalculation`** · "work rule"→**"business
> rule"** · `OrganizationSettings`→`organizationSettings`. **`process-step-action`** modeline Action'ın (`organizationId` hariç) **tüm alanları
> tanımlarıyla** eklendi. **`InstanceValue` PK += `serviceId`** (`(instanceId, serviceId)`; models/index.md dahil). **`reflectionMode`**
> davranış dokümanına (`properties.md` §2.5/§3.14/§3.16) yayıldı. **Düzeltmeler:** `style.md` kırık link · `company.md` `Position` ters-ilişki ·
> `new-vs-current.md` bayat sayılar (18/22) · todo O6 mükerreri · engine §2.2 tech-kararı nüansı · `README.md` yeniden yazıldı · compare §11'e
> değer-saklama katmanı. Örneklere (`sampleProcess/`) **dokunulmadı** (kullanıcı kararı). Kabul/ertelenen: SchedulerJob `...At` · BusinessRuleCondition
> ikili yuvalama · Style `name`. **~24 dosya.** **Bu dosya kapandı; sonraki sürüm `v0-30.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Bekleyen açık konular (`todo.md`):** **O3** döngü koruması — A→B→A tetikleme recursion'ı için **motor-seviyesi** güvenlik ağı
> (derinlik/hop sınırı) · **O6** dış referans anahtarı `organizationId` vs `organizationCode` (Customer API detaylanınca) ·
> ServiceTrigger **`timer` saat dilimi/DST** · **`async` kaskad kompozisyonu/derinlik sınırı** · **`triggerProcessStep →
> associatedInstance`** alt süreç/aksiyon tetikleme tanımı · **D3** satır↔ekstre iş kuralı ↔ alt-süreç kalıcılık kararı ·
> **Property value — model + A′ yansıma İŞLENDİ; `propertyValuesTemplates/` baştan analiz edildi (v0.28 — 9 bulgu çözüldü).**
> Açık operasyonel kararlar: **rollup/denormalize dashboard** · **A′ yayılım derinlik/döngü (O3) + `sync` fan-out eşiği** (yalnız
> sayısal) · **retention/pruning + KVKK** · **davranış-dokümanı entegrasyonu** (`properties.md`/`flovo-bpm-engine.md` yazma/okuma
> yolu + JSON Schema) · GIN/tipli index stratejisi · **`apiKeyId`** içeriği (Customer API) · Form **`validated`/`FormValidation`** ·
> **loglama modeli** (`research/settings-log/`; sistem logları açık) · **ortam (env) / servis template** · **Üst Form Kullanıcı
> (§3.22) kenar durumları** · Form List tik & filtreler · motor iç mekaniği (orkestrasyon↔yürütme, Tier 1) · **adım ayar detayları**
> (Flovo AI · `settings` JSONB doğrulama · `triggerProcessStep`/`formRedirect` ayarları · `DynamicParameter.value` şekli) ·
> **`ActionTransfer.user`** ile creatorUserId opsiyonel atama · **`idleTimeoutMinute`** alt/üst sınırı + Organization sonraki alanları.

---

> **Kaynak:** **Tüm-proje baştan tutarlılık analizi** (5 paralel okuyucu: models · service-settings · organization-settings · kök+todo · sampleProcess+compare) → bulgular önceliklendirildi ve kullanıcı kararlarıyla uygulandı. Örneklere (`sampleProcess/`) kullanıcı isteğiyle **dokunulmadı**.

## 🆕 Eklenenler
- **`ProcessStepAction` kopyalanan alan tablosu (tam tanımlı):** modelde şablondan kopyalanan Action alanları yalnız isim listesiydi; artık `code`·`definition`·`translationCode`·`icon`·`styleId`·`actionType`·`validation`·`stayOnPage`·`showInHistory`·`actionDisplayType` **tanımlarıyla** bu sayfada (Action'ın `organizationId` dışındaki tüm alanları). `models/service-settings/process-step-action.md`.
- **compare §11'e "Değer saklama katmanı (v0.26–v0.28)" alt-bölümü:** `InstanceValue`/`InstanceAttr`/`InstanceListItem`/`Outbox`/`Change`/`LabeledValue` + A′ yansıma (`reflectionMode`/`reflectionPropagation`, ReflectionLink yok → AssociatedInstance+metadata, röle/live ata-referansı) + `data` anahtar konvansiyonu + 18 şablon. `research/compare/new-vs-current.md`.

## 🔧 Değişenler / kararlar
- **`reflectionMode` davranış dokümanına da işlendi (S1/S2):** `properties.md` §3.14 (flowInfo) / §3.16 (userInfo)'ya `reflectionMode` eklendi; §2.5 ilişki-listesine `reflectionMode`/`reflectionPropagation` eklendi (§3.15'in §2.5'e çapraz-atıf çelişkisi giderildi). _(v0.28 kararının davranış-dokümanı yayılımı eksikti.)_
- **KARAR — geçmiş alanları iki AYRI alan (Action + ProcessStepAction):** **`showInHistory`** (öğe kullanıcının geçmiş görüntülemesinde **görünür** mü) + **`showHistory`** (aksiyon **tamamlanınca** kullanıcıya **akış tarihçesini otomatik göster**) — farklı kavramlar, **birleştirilmez**. ProcessStep `hideInHistory`→**`showInHistory`** (pozitif polarite, vars. `true`; ProcessStep'te `showHistory` yok). _(İlk turda yanlışlıkla tek ada indirgenmişti → düzeltildi; research/current = eski-app referansı korundu.)_
- **KARAR — `fromCalculate`→`fromCalculation` (S4):** aynı "ifade sonucu" kavramı tek yazımda; `BusinessRuleConditionCompareType` + business-rule/-condition dokümanları + enums/index.
- **KARAR — "work rule"→"business rule" (S5):** eski ad `work rule`/`work-rule` yeni dokümanlarda `business rule`'a çevrildi (business-rule.md + todo.md 3 atıf). _(research/current = eski, korundu.)_
- **KARAR — `InstanceValue` PK'sine `serviceId` (M2):** `(instanceId, serviceId)`; partition kolonu PK'de (HASH gereği) + `InstanceAttr`/`InstanceListItem` simetrisi + servis-kapsamlı join'siz sorgu.
- **`flovo-bpm-engine.md` §2.2 dağıtım kararları güncellendi (R1/R2):** "(açık)" yerine **teknoloji kararı verildi** (NATS kuyruk+durumsuz worker · on-prem+Private Cloud ready → tech-stack); **motor-içi orkestrasyon↔yürütme / operasyon detayı** açık kalır (→ §12/todo).

## 🩹 Düzeltmeler
- **Kırık link (O1):** `style.md` `../../models/index.md` → `../models/index.md`.
- **Casing (O2):** `permissions.md` `OrganizationSettings` → `organizationSettings`.
- **Ters-ilişki eksik (M1):** `company.md` `companyId` tüketicilerine `Position` eklendi.
- **Bayat sayılar (C1–C3):** `new-vs-current.md` `ImageAreaSelector` "korunan"dan çıkarıldı · "19 alan (16+3)" → **18 (16+2)** · "ProcessStepType (21)" → **22** (kendi başlık/footer'larıyla çelişki giderildi).
- **todo O6 mükerreri (R5):** "🆕 eklenen" bölümündeki Customer API org id/code sorusu Tier 2'ye konsolide edildi.
- **engine durum satırı (R6):** bekleyen bölümlere §3/§7 eklendi.
- **bayat "Açık konu" (S6):** `properties.md` §3.13 Form List notu → view-profile çözümü + kalan açık ayarlar todo işaretçisi.
- **doküman-hijyeni (M5):** `reflection-propagation.md` başlık durumuna v0.28 (§9 ata-referansı + §3 yön nüansı) eklendi.
- **`README.md`:** Azure DevOps boilerplate → proje kimliğine uygun Türkçe tanıtım (kod-değil-doküman, içerik haritası).
- **Rename-sonrası re-analiz düzeltmeleri:** **(a)** `models/index.md` `InstanceValue` PK'si `instanceId [PK=FK]` → **`(instanceId, serviceId)`** (M2'nin index'e yansımayan yeri — ilişki haritası + FK tablosu); **(b)** `new-vs-current-names.md`'ye geçmiş alanları kaydı: `hideInHistory`→`showInHistory` + **`showInHistory` ≠ `showHistory`** (iki ayrı alan). Hedefli tarama: `showInHistory` 3 varlıkta tutarlı · dangling `hideInHistory`/`fromCalculate`/`work-rule` yok · kırık link yok.

## ❓ Yeni açık sorular
_(yok — analiz bulguları ya düzeltildi ya kullanıcı kararıyla kapsam-dışı bırakıldı.)_

## ✅ Çözülen açık sorular
- **Tüm-proje audit bulguları:** yukarıdaki S1–S6/O1–O2/M1–M2/C1–C3/R1–R6 uygulandı. **Kabul edilen/ertelenenler** (değişmedi): `SchedulerJob` `...At` adlandırma (bilinçli altyapı-erteleme) · `BusinessRuleCondition` ikili yuvalama (iş kuralı fazında) · Style `name` vs `definition` (Style çevrilmez) · `sampleProcess/` Türkçe kodlar (kullanıcı: örneklere dokunma) · compare/ v0.27–v0.28 kararları (eklendi).

---

*Commit başlığı: **v0.29**.*
