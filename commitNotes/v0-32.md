# v0.32 — İnsan-görev ailesi ortak modeli çözüldü (grup-onay yok · dinamik üyelik · atama-hata fallback · eskalasyon=timeout-hedefi · görev-devri yok→vekalet) + Üst Form Kullanıcı kenar durumları (read-only kuralı) + Vekalet açık maddesi

> **Önceki:** [`v0-31.md`](./v0-31.md)
> **Kapsam:** **İnsan-görev ailesi ortak modeli** kapatıldı — Kullanıcı / Kullanıcı Grubu / Üst Form Kullanıcı / Processing
> **ortak iskeleti** paylaşır (adım tipleri **ayrı kalır**); politikalar: **grup-onay eşiği yok** (tek üye) · **grup üyeliği
> dinamik** (`userGroupId` okuma-anı) · **atama çözülemezse hata/`onFail`** · **eskalasyon = timeout aksiyonu hedefi** (no-code) ·
> **görev-devri yok → vekalet**. **Üst Form Kullanıcı (§3.22) kenar durumları** çözüldü (üstte aksiyon alabilen yoksa alt kayıt
> **read-only** · birden fazla üst → **ilk tespit edilen** · atananlar **kopyalanmaz**, canlı okunur) + "asıl amaç" gerekçesi
> zenginleştirildi. **Yeni açık madde: Vekalet (proxy) sistemi** (ayrıntı sonra). **~7 dosya.** **Bu dosya kapandı; sonraki
> sürüm `v0-33.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Bekleyen açık konular (`todo.md`):** **İki-katman sınırı** (değer atama & karşılaştırma → iş kuralı=frontend UX ↔
> adım=kalıcı/akış kararı; iş kuralı en son şekillenecek) · **Veri modeli — temsil ÇÖZÜLDÜ (v0.30, koleksiyon-tabanlı);**
> açık kalan: **soy ağacı (lineage)** + **çok-kayıt iterasyonu / loop / join** (item-dizisi fan-out → paralel dallanma) ·
> **Vekalet (proxy) sistemi** (görev-devri yerine kalıcı vekalet; `UserProxy` model + kapsam + iz/log + zincir kararları —
> ayrıntı sonra) · **O3** döngü koruması (derinlik/hop sınırı) · **O6** dış referans anahtarı
> `organizationId` vs `organizationCode` (Customer API) · ServiceTrigger **`timer` saat dilimi/DST** · **`async` kaskad
> kompozisyonu/derinlik sınırı** · **`triggerProcessStep → associatedInstance`** tetikleme tanımı · **D3** satır↔ekstre iş
> kuralı ↔ alt-süreç kalıcılık kararı · **`apiKeyId`** içeriği (Customer API) · **loglama modeli**
> (`research/settings-log/`; sistem logları açık) · **ortam (env) /
> servis template** · Form List tik & filtreler · motor iç mekaniği
> (orkestrasyon↔yürütme, Tier 1) · **adım ayar detayları** (Flovo AI · `settings` JSONB doğrulama · `triggerProcessStep`/
> `formRedirect` ayarları · `DynamicParameter.value` şekli) · **`ActionTransfer.user`** ile creatorUserId opsiyonel atama ·
> **`action` nesnesi şekli** (HTTP response tetikleme paketi) · **`idleTimeoutMinute`** alt/üst sınırı + Organization sonraki
> alanları · **`BusinessRuleCondition` ikili yuvalama** (iş kuralı fazında) · **`SchedulerJob` `...At` adlandırma** (altyapı-erteleme).

---

## 🆕 Eklenenler
_(henüz yok)_

## 🔧 Değişenler / kararlar
- **KARAR — İnsan-görev ailesi ortak modeli:** Kullanıcı (§3.15) · Kullanıcı Grubu (§3.16) · Üst Form Kullanıcı (§3.22) ·
  Processing (§3.18) **ortak insan-görev iskeletini** paylaşır (atama `InstanceAwaitingUser` → bekle → bildirim + timeout +
  görüntüleme profili); fark yalnız **atananın çözümü** + **aksiyon-alınabilirlik**. Adım tipleri **ayrı kalır** (yapı
  planlandığı gibi). İşlendi: `flovo-bpm-engine.md` §4.3 (ortak davranış notu) + §6.2 · `service-settings/process-step.md`
  §3.15/§3.16/§3.22 + §4 log · `models/service-settings/process-step.md` (§3.22 notu) · `models/enums/process-step-user-group-type.md` ·
  `models/processInstances/instance-awaiting-user.md` · `todo.md`.
  - **Grup-onay eşiği YOK:** gruptan **tek üye** onayı yeterli; quorum/"hepsi onaylar" yok (kalıcı). → `process-step.md` §3.16 ·
    `instance-awaiting-user.md`.
  - **Grup üyeliği DİNAMİK:** `InstanceAwaitingUser.userGroupId` okuma-anı çözülür → gruba eklenen/çıkan üye otomatik yansır. →
    `instance-awaiting-user.md`.
  - **Atama çözülemezse → hata/fallback:** `userType`/`userGroupType` boş dönerse adım `onFail`'e düşer (sessiz atanansız bekleme yok). → `process-step.md` §3.15.
  - **Eskalasyon = timeout aksiyonu hedefi:** ayrı mekanizma yok; timeout aksiyonunun `targetProcessStepId`'si yönetici adımı
    yapılarak no-code sağlanır. → `flovo-bpm-engine.md` §6.2.
- **KARAR — Üst Form Kullanıcı (§3.22) kenar durumları çözüldü:** üstte aksiyon alabilen yoksa (bulunamadı / otomatik adım /
  Süreç Bitişi) → alt kayıt **herkese read-only** (profil: code-eşleşmesi → yoksa `isDefault`); **birden fazla** üst → **ilk
  tespit edilen**; atananlar **kopyalanmaz**, üstten **canlı** okunur. Ayrıca §3.22 "asıl amaç" gerekçesi (tekrar/hata önleme +
  aksiyon-tetikleme ihtiyacının kalkması + performans) zenginleştirildi. İşlendi: `process-step.md` §3.22.

## 🩹 Düzeltmeler
_(henüz yok)_

## ❓ Yeni açık sorular
- **Vekalet (proxy / yetki verme) sistemi** — görev-devri yerine kalıcı vekalet: kullanıcı başkalarına vekalet verir, **vekil
  vekaleti verenin yerine geçerek onun adına işlem yapar**. `todo.md`'ye açık madde olarak eklendi; **ayrıntı sonra** (model
  `UserProxy` + kapsam tüm-servis↔servis-bazlı + iz/log "X adına Y" + grup görevlerini kapsama + zincir/tek-kademe).

## ✅ Çözülen açık sorular
- **İnsan-görev ailesi ortak modeli (Tier 2)** → çözüldü (yukarı bkz.): ortak iskelet + grup-onay yok + dinamik üyelik + atama-hata
  fallback + eskalasyon=timeout-hedefi + görev-devri yok→vekalet.
- **Üst Form Kullanıcı (§3.22) kenar durumları (Tier 2)** → çözüldü (a–e + b): read-only kuralı + ilk-tespit + canlı okuma.

---

*Commit başlığı: **v0.32**.*
