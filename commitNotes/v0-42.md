# v0.42 — Dökümantasyon ↔ pilot tutarlılık düzeltmeleri (v0.41-1 sonrası): servis versiyonlama/arşivleme modellendi · RLS "Pattern B v2" birleştirildi · draft/publish "açık"→"inşa edildi"

> **Önceki:** [`v0-41.md`](./v0-41.md) · **Ara pilot-geçişi:** [`v0-41-1.md`](./v0-41-1.md) (uygulama reposu `flovo-ibpm-v2` PR'ı: `implementation-status.md` + tech-stack canlı-işaretleri).
> **Kapsam:** Pilot **v0.41-1** geçişinden sonra tasarım dokümanları taranıp **pilot gerçeğiyle uyumsuz/eksik** yerler düzeltildi (8 dosya,
> 108 link → 0 kırık). **(A)** Pilotta **inşa edilmiş** ama tasarımda "açık/yok" olan konular güncellendi: `service.md`'ye **servis
> versiyonlama** (`ServiceVersion` · `currentVersion`/`hasUnpublishedChanges`/`lastPublishedAt` + code-lock) ve **süreç arşivleme**
> (`archivedAt`/`archivedBy` + `ArchivedChecker` cross-domain guard + `archiveFilter`) alan+bölüm olarak eklendi; `settings-api.md §7`
> "(açık)" → **"pilotta inşa edildi"**; `todo.md` ilgili maddeleri güncellendi; `bpm-engine-build-plan.md`'ye pilot-gerçeği bloğu eklendi.
> **(B)** Tutarsızlıklar: **"Pattern B" → "Pattern B v2"** 4 dokümanda birleştirildi; `keycloak.md` RLS mekanizması gerçekle uzlaştırıldı
> (`SET app.organization_id` → **tenant GUC + `active_tenant_id()`**, branch-siz). **(C)** Eksikler: `postgresql.md`'ye **"Pattern B v2"
> tanımı** (GUC-native · branch-siz · **insan + AI-agent parity**); `service.md`'ye **`Process ≡ Service` (1:1)** pilot kapsam-sınırı notu.
> **Bu dosya kapandı; sonraki sürüm `v0-43.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Uygulanmayan (bilinçli):** BFF server-side bearer injection (C4) — düşük öncelik; `login-register-plan.md` Faz 1 yazımında eklenir.
>
> **Sıradaki adaylar (`bpm-engine-build-plan.md`):** **F1.A.1** (`workflow_events` model dosyası) ile Faz 1 yazımı · Settings API
> request/response derinleştirme · `ServiceVersion` snapshot içeriği + ortamlar-arası kopya (env modeli).
>
> **Bekleyen açık konular (`todo.md`):** Settings API kalan (hata sözleşmesi · ortamlar-arası kopya · toplu senkron · loglama · yetki
> granülaritesi) · motor runtime kalan (workflow_events/projection model · scheduler lider-seçim · retry/global-hata/compensation · fork/join ·
> pruning/KVKK) · İki-katman sınırı (Tier 0) · Ortam (env) modeli · Vekalet (proxy) · İş kuralı kalan detaylar · adım ayar detayları.
>
> **Not:** Ana dizin plan/durum dosyaları → [`../bpm-engine-build-plan.md`](../bpm-engine-build-plan.md) (motor geliştirme) ·
> [`../login-register-plan.md`](../login-register-plan.md) (kimlik & erişim) · [`../bpmEngine/implementation-status.md`](../bpmEngine/implementation-status.md) (tasarım ↔ inşa/dağıtım durumu).

---

## 🆕 Eklenenler
- **`models/service-settings/service.md` — Versiyonlama & Arşivleme:** alan tablosuna `currentVersion` · `hasUnpublishedChanges` ·
  `lastPublishedAt` · `archivedAt` · `archivedBy`; **"Versiyonlama & yayınlama (draft/publish)"** bölümü (`ServiceVersion` model özeti +
  code-lock) + **"Arşivleme"** bölümü (`ArchivedChecker` read-only cross-domain guard + `archiveFilter`).
- **`postgresql.md` — "Pattern B v2" tanımı:** GUC-native (`active_tenant_id()`) · branch-siz · **insan + AI-agent parity by-construction**.

## 🔧 Değişenler / kararlar
- **RLS terminoloji birleştirme:** **"Pattern B" → "Pattern B v2"** — `settings-api.md` · `engine-runtime.md` ·
  `service-settings/business-rule-endpoints.md` · `keycloak.md` (2 yer).
- **`keycloak.md` RLS mekanizması** gerçekle uzlaştırıldı: `SET app.organization_id` → her istekte **tenant GUC** set + RLS politikası
  `active_tenant_id()` ile okur (GUC-native, branch-siz).
- **`settings-api.md`:** §7 "Draft/yayınlama & versiyonlama **(açık)**" → **"(pilotta inşa edildi)"** (draft→publish + versiyonlama +
  arşivleme akışı, implementation-status atfı); §9 açık-nokta listesi "draft/publish+versiyonlama" → **"ortamlar-arası kopya/promote"**.
- **`todo.md`:** Settings API açık-nokta maddesine "pilotta inşa edildi (v0.41-1)" alt-notu (versiyonlama+arşivleme, env'den bağımsız);
  "Ortam modeli" maddesine versiyonlamanın **env'siz** inşa edildiği notu.
- **`bpm-engine-build-plan.md`:** §2 başına pilot-gerçeği bloğu — design-time katman **BUILT+deployed**, her iki fazın üstünde (upstream);
  Bölüm-1 ↔ Faz numaralandırma uzlaştırması.

## 🩹 Düzeltmeler
- **`service.md`** Notlar: ertelenen alan listesinden "versiyon" çıkarıldı (artık modellendi); **`Process ≡ Service` (1:1)** pilot
  kapsam-sınırı notu eklendi (tasarım hedefi = alt-süreç ile çok-ProcessInstance korunur).

## ❓ Yeni açık sorular
- **`ServiceVersion` snapshot içeriği** — yayınlanan tanımın hangi alanları saklanır (todo "Ortam modeli"ne bağlı; env kopya ile birlikte
  netleşecek).

## ✅ Çözülen açık sorular
- **Draft/publish + servis versiyonlama — ÇÖZÜLDÜ (pilotta inşa; v0.41-1 doğrulandı, v0.42 dokümana işlendi):** ortam modelinden
  **bağımsız** inşa edildi (`ServiceVersion` + code-lock); tasarım "env modeliyle bağlı" varsayımı gevşedi. → `service.md` · `settings-api.md §7`.
- **Süreç arşivleme — ÇÖZÜLDÜ (pilotta inşa; v0.42 dokümana işlendi):** soft + geri-alınabilir (`archivedAt`/`archivedBy`) +
  `ArchivedChecker` cross-domain guard + `archiveFilter`. → `service.md`.
- **RLS "Pattern B" ↔ "Pattern B v2" terminoloji tutarsızlığı — ÇÖZÜLDÜ (v0.42):** tüm dokümanlar "Pattern B v2"ye birleştirildi;
  mekanizma `active_tenant_id()` GUC ile tanımlandı.

---

*Commit başlığı: **v0.42**.*
