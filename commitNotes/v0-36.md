# v0.36 — Servis + organizasyon ayar modelleri gözden geçirildi ve TASLAK'tan çıkarıldı (30 model → 🟢) + `SyncStatus` enum (synchronizationStatus bool→enum tekleştirme, 9 org text-entity) + BLOKER'lar giderildi (property `associatedServiceId` · tanımsız enum · gövde review-artefaktı temizliği) + actionType `delete` + tutarlılık düzeltmeleri

> **Önceki:** [`v0-35.md`](./v0-35.md)
> **Kapsam:** Servis + organizasyon ayarı **veri modelleri** (30 dosya) 6 paralel denetim + 3 düzeltme ajanıyla gözden
> geçirilip **TASLAK → 🟢 Gözden geçirildi (v0.36)** yapıldı. Giderilen gerçek eksikler: `property.associatedServiceId`
> çekirdek FK · tanımsız **`SyncStatus` enum** (oluşturuldu; `synchronizationStatus` **bool→enum** 9 org text-entity'de
> tekleştirildi) · `worker-level`/`vacation-day` gövdesine sızmış **review-log/persona artefaktları** temizlendi ·
> `actionType`'a **`delete`** eklendi · çeşitli tutarlılık/senkron düzeltmeleri. **970 iç linkten 0 kırık.**
> **Bu dosya kapandı; sonraki sürüm `v0-37.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Bekleyen açık konular (`todo.md`):** **İki-katman sınırı** (değer atama & karşılaştırma → iş kuralı=frontend UX ↔
> adım=kalıcı/akış kararı) · **İş kuralı — kalan detaylar** (motor v0.35'te büyük ölçüde çözüldü [JSONLogic · propertyType-karşılaştırma +
> `containsAny`/`containsAll` · gömülü-JSONB koşul · hibrit prepareEngine · readonly]; **kalan:** JSONLogic **katalog operatör imzaları** ·
> `environmentRestriction` değerleri · hata görünürlüğü + veri-hatası · cascade **döngü/derinlik limiti** · `search` değer kaynağı ·
> `setStyle` `style`-şeması · `showMessage` bir-kez-göster · `httpRequest` alan-yolu JSONPath) · **Veri modeli — temsil ÇÖZÜLDÜ (v0.30,
> koleksiyon-tabanlı);** açık kalan: **soy ağacı (lineage)** + **çok-kayıt iterasyonu / loop / join** · **Vekalet (proxy) sistemi**
> (`UserProxy` — ayrıntı sonra) · **O3** döngü koruması · **O6** dış referans anahtarı `organizationId` vs `organizationCode` (Customer API) ·
> ServiceTrigger **`timer` DST kenar durumları** · **`async` kaskad kompozisyonu/derinlik sınırı** · **`triggerProcessStep →
> associatedInstance`** tetikleme tanımı · **D3** satır↔ekstre iş kuralı ↔ alt-süreç kalıcılık kararı · **`apiKeyId`** içeriği (Customer API) ·
> **loglama modeli** (`research/settings-log/`; sistem logları açık) · **ortam (env) / servis template** · Form List tik & filtreler ·
> motor iç mekaniği (orkestrasyon↔yürütme, Tier 1) · **adım ayar detayları** (Flovo AI · `settings` JSONB doğrulama ·
> `triggerProcessStep`/`formRedirect` ayarları · `DynamicParameter.value` şekli) · **`ActionTransfer.user`** ile creatorUserId opsiyonel
> atama · **`SchedulerJob` `...At` adlandırma** (altyapı-erteleme).
>
> **Not:** Login/register (kimlik & erişim) işleyiş dökümantasyonu için yol haritası → ana dizinde [`login-register-plan.md`](../login-register-plan.md)
> (§6 kararları verilince Faz 1'den yazım başlar).

---

## 🆕 Eklenenler
- **`SyncStatus` enum** (`models/enums/sync-status.md`, YENİ) — org-ayarı text-entity'lerinin `synchronizationStatus` alanı için
  3-state string-enum (`synced`/`pending`/`error`); `enums/index.md`'ye satır + bool→enum tekleştirme notu eklendi.

## 🔧 Değişenler / kararlar
- **Servis + organizasyon ayar modelleri gözden geçirildi ve TASLAK'tan çıkarıldı (30 model → 🟢 v0.36).** 6 paralel
  denetim ajanı (kapsam · iç/çapraz tutarlılık · İngilizce isim/PK/FK/enum · kırık link · açık-soru yerleşimi) + 3 düzeltme
  ajanı. **Servis (11):** service · solution · property · property-item · process-step · process-step-action · service-trigger ·
  view-profile ailesi (3) · business-rule (model kabuğu). **Org (19):** action · status · style · translation · organization ·
  user · user-group · worker-level · additional-qualification · position · profession · company · department · cost-center ·
  credit-card · working-schedule · vacation-day · scheduler-job · process-transfer. **970 iç linkten 0 kırık.**
- **`synchronizationStatus` tip tekleştirme (bool → `SyncStatus` enum):** worker-level · vacation-day · company · department ·
  cost-center · credit-card · position (Position + Staff) · profession · user — hepsi tek tip `SyncStatus` enum'a hizalandı.
- **`property.md`:** §1.5'e eksik **`associatedServiceId`** (FK → Service) çekirdek alanı eklendi (BLOKER giderildi);
  `backingField`/`state`/`dataSource*` tipsiz alanları somutlandı.
- **`action.md` + `process-step-action.md`:** satır-içi `actionType` listesine kanonik enum'un 8. değeri **`delete`** eklendi.
- **`service-trigger.md`:** tenant (`organizationId` `serviceId`'den türetilir) notu + iç-içe backtick render düzeltmesi.

## 🩹 Düzeltmeler
- **Süreç-artefaktı temizliği (`worker-level.md` · `vacation-day.md`):** gövdeye sızmış review-log / persona metinleri
  (`James-SA-catch`, `Emma cross-cutting-inventory`, `BO-directive`, `v-next align`, `VAC-cluster`, olmayan `CHANGES.md` atfı)
  ve `[YENİ]`/`[DEĞİŞİM]` changelog işaretleri kaldırıldı — dokümanlar temiz **mevcut-durum** anlatımına döndürüldü.
- **`business-rule.md`:** gövdedeki 4 eski-motor karşılaştırma parantezi (CLAUDE.md kural 4) sadeleştirildi.
- **`view-profile.md`:** çözülmüş "servis-bazlı mı paylaşımlı mı" açık-soru notu **ÇÖZÜLDÜ (servis-bazlı)** olarak netleştirildi;
  davranış dokümanı `service-settings/view-profile.md` §1 tablosuna `translationCode` satırı eklendi (model ile senkron).
- **`process-step.md`:** §3.16 `formRedirect` satırına todo işaretçisi (triggerProcessStep ile simetrik).
- **`scheduler-job.md`:** `lastRunAt`/`lastRunStatus` nullable + altyapı-erteleme işaretçisi; **todo.md Tier 3'e** `...At`/`...Time`
  adlandırma birliği maddesi eklendi.
- **`process-transfer.md`:** kalıcı vekalet (proxy) ile uzlaştırma notu + sarkan denetim-kaydı işaretçisi loglama modeline bağlandı.

## ❓ Yeni açık sorular
- **`SchedulerJob` altyapı modeli** (`...At`/`...Time` adlandırma + serbest-string enum adayları) → todo.md Tier 3 (erteleme).

## ✅ Çözülen açık sorular
- **`synchronizationStatus` tip sürüklenmesi (bool ↔ enum) — ÇÖZÜLDÜ (v0.36):** tüm org-ayarı text-entity'lerinde tek tip
  **`SyncStatus` enum** (`synced`/`pending`/`error`); `models/enums/sync-status.md` oluşturuldu + index'e alındı.
- **Servis + org ayar modelleri "TASLAK → gözden geçirilmiş" dondurma — ÇÖZÜLDÜ (v0.36):** 30 model denetlendi, gerçek
  eksik/tutarsızlıklar (property `associatedServiceId` · `SyncStatus` tanımsız enum · gövde-artefaktları · `delete` eksik) giderildi.

---

*Commit başlığı: **v0.36**.*
