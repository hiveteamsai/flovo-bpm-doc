# v0.17 — `RelatedInstance` → `AssociatedInstance` yeniden adlandırması + `form` tipinde `Instance.creatorUserId` "zorunlu dolu" kuralının kaldırılması

> **Önceki:** [`v0-16.md`](./v0-16.md)
> **Kapsam:** **(1) Rename:** `RelatedInstance` → **`AssociatedInstance`** (dosya + `relatedInstanceId`→`associatedInstanceId`
> · `relatedPropertyId`→`associatedPropertyId`); 8 dosyadaki referanslar güncellendi; eski-Flovo adları
> (`RelatedForm`/`relatedFormId`), Property'nin `relatedPropertyIds` (çoğul) alanı ve `unlinkRelated` enum değeri korundu;
> compare footer'larına v0.17 kaydı. **(2) Düzeltme:** `form` tipi serviste `Instance.creatorUserId` **"zorunlu dolu"**
> kuralı kaldırıldı — süreç API/webhook ile (tek oluşturan kullanıcı olmadan, ör. `referred`) başlatılabildiğinden
> `creatorUserId` **null olabilir**; başlatan `ProcessInstance.createdByApiKeyId` ile izlenir. `todo.md`'deki ilgili madde
> daraltıldı (opsiyonel `ActionTransfer.user` atama açık kaldı). Bu dosya **kapandı**; sonraki sürüm `v0-18.md`.
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir.
>
> **Bekleyen açık konular (`todo.md`):** Property value **depolama modeli** (araştırma + ölçekli rating + tech-stack
> substratı hazır → `research/property-value-storage/` + `tech-stack/`; **karara bağlanıp `models/` PropertyValue +
> `property.md`'ye işlenecek**) · **`apiKeyId`** içeriği (Customer API) · grup-onay **`groupApproval`** çakışması ·
> Form **`validated`/`FormValidation`** · **loglama modeli** (Tier 1) — **ayar logu tasarımı hazır, karar bekliyor**
> → [`research/settings-log/`](../bpmEngine/research/settings-log/index.md) _(ön koşul: Customer API ayar-yazma ucu ·
> açık: KVKK/saklama · `requestBody` eşiği · `HttpMethod`'a `patch`)_; **sistem logları** için hâlâ hiçbir karar yok ·
> **ortam (env) / servis template** · alt-servis `parentViewProfile` · Form List tik & filtreler ·
> **`valueType` isim çakışması** (opsiyonel) · motor iç mekaniği — orkestrasyon↔yürütme (Tier 1) ·
> **adım ayar detayları:** Flovo AI ayarları · `settings` JSONB doğrulama/bütünlük · `triggerProcessStep`/`formRedirect`
> ayarları · `DynamicParameter.value` şekli · **`translationCode` ad-uzayı kuralı** ·
> **v0.15'ten:** süreç-başlatma **`userGroupId`** davranış tarafı (kim başlatabilir §3.1'e yazılacak) ·
> **`Instance.delete`↔`deleted`** isim tutarsızlığı (kanonik `deleted`'e sabitlenecek — kontrol bekliyor).

---

## 🆕 Eklenenler
_(henüz yok)_

## 🔧 Değişenler / kararlar
- **`RelatedInstance` → `AssociatedInstance` (rename, KARAR):** model adı daha mantıklı bulundu. Dosya
  `related-instance.md` → `associated-instance.md` (git rename); alanlar **`relatedInstanceId` → `associatedInstanceId`**,
  **`relatedPropertyId` → `associatedPropertyId`**. **Güncellenen referanslar:** `models/processInstances/`
  (`associated-instance.md` içerik + `index.md` + `instance.md` ilişki satırı) · `models/index.md` (klasör satırı + ağaç +
  ilişki tablosu + runtime özeti) · `models/service-settings/process-step.md` (Instance Deleter) · `models/enums/instance-delete-mode.md`
  (2 not) · `todo.md` · `research/compare/new-vs-current.md` + `new-vs-current-names.md` (yeni-taraf + v0.17 footer kaydı).
  **Dokunulmadı:** eski-Flovo adları `RelatedForm`/`relatedFormId` (tarihî, compare sol-taraf) · Property modelindeki
  **`relatedPropertyIds`** (çoğul — *parentProperty* alanı, farklı kavram) · `unlinkRelated` enum değeri.

## 🩹 Düzeltmeler
- **`form` tipi serviste `Instance.creatorUserId` "zorunlu dolu" kuralı kaldırıldı.** `referred` örneği süreci **API/webhook**
  ile başlatıp bir gruba yönlendirdiğinden atanacak **tek bir oluşturan kullanıcı yok**; `creatorUserId` (zaten `int?`)
  **null olabilir**, başlatan **`ProcessInstance.createdByApiKeyId`** ile izlenir (ProcessInstance zaten "createdByUserId
  **veya** createdByApiKeyId biri dolar" diyordu — Instance tarafı buna hizalandı). **Düzeltilen yerler:**
  `models/service-settings/service.md` (§`form`) · `models/enums/form-type.md` · `models/processInstances/instance.md`
  (alan notu + prose) · `models/index.md` (ilişki notu + formType özeti) · `sampleProcess/referred/referred.md` (Form
  Creator adımına not).

## ❓ Yeni açık sorular
_(henüz yok)_

## ✅ Çözülen açık sorular
- **API-başlatımlı `creatorUserId` tespiti (`todo.md`) — ÇÖZÜLDÜ (kısmen):** "form tipinde creatorUserId zorunlu dolu"
  yanlıştı; API/webhook başlatımında **null** kalır, başlatan `createdByApiKeyId` ile izlenir. Açık kalan: yalnız
  `ActionTransfer.user` ile creatorUserId'nin **opsiyonel atanması** (todo maddesi bu kısma daraltıldı).

---

*Commit başlığı (atılınca): **v0.17**.*
