# v0.38 — Tipe-özel Process Step ayar şemaları (22 stepType → `jsonTemplateModels/process-step-settings/`: settings + alt-modeller + JSON Schema) — böylece tipe-özel `settings` şeması property (18) + process-step (22) ile TAMAM

> **Önceki:** [`v0-37.md`](./v0-37.md)
> **Kapsam:** 22 `stepType` için tip-başına **`ProcessStep.settings` (JSONB) ayar şeması** oluşturuldu
> (`.../jsonTemplateModels/process-step-settings/`, 20 dosya): §1 settings tablosu (amaç-odaklı açıklamalar) + §2 alt-modeller
> (recursive `ComparisonCondition` · SendNotification* ailesi · Timer*Schedule) + §3 ortak alanlar + JSON Schema
> (`additionalProperties:false`, cross-file `$ref`). `notification.md`/`timer.md` paylaşılan alt-modelleri kanonik tanımlar;
> `user`/`userGroup` cross-ref. Ayarsız (`subProcess*`) + ertelenmiş (`triggerProcessStep`/`formRedirect`) tipler dürüstçe işaretlendi.
> **Bununla "tipe-özel `settings` JSON Schema" işi property (18) + process-step (22) ile TAMAMLANDI.**
> **1293 iç linkten 0 kırık. Bu dosya kapandı; sonraki sürüm `v0-39.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Kilometre taşı (v0.36–v0.37):** Servis + org ayar **modelleri** gözden geçirilip TASLAK'tan çıkarıldı (v0.36);
> **Property tipe-özel `settings` şemaları** 18 tip için yazıldı (v0.37, `jsonTemplateModels/property-settings/`).
> **Sıradaki doğal adım:** **process-step tipe-özel `settings` şemaları** (22 adım tipi; `ProcessStep.settings` doğrulama
> kapısı) — eksik `triggerProcessStep`/`formRedirect`/`flovoAi` ayarları da burada kapanır.
>
> **Bekleyen açık konular (`todo.md`):** **process-step tipe-özel `settings` JSON Schema'ları** (22 adım) · **Ayar-yazma
> (design-time CRUD) API yüzeyi** (service/property/step/view-profile) · **Property `settings` açık alan kararları**
> (`flowInfoValue`/`userInfoValue` enum · Form List red-akışı bayrakları · groupByTax eski-kod adayları) · **İki-katman sınırı** ·
> **İş kuralı — kalan detaylar** (JSONLogic katalog operatör imzaları · `environmentRestriction` · hata görünürlüğü · cascade
> döngü/derinlik · `search` · `setStyle` şeması · `showMessage` bir-kez-göster · `httpRequest` JSONPath) · **Motor runtime
> mimarisi** (§2.2 orkestrasyon↔yürütme · §8 kalıcılık/durum yaşam döngüsü · §4.5 paralel dallanma/join) · **Veri modeli** açık:
> soy ağacı (lineage) + çok-kayıt iterasyonu/loop/join · **Vekalet (proxy)** (`UserProxy`) · **O3** döngü koruması · **O6** dış
> referans anahtarı (Customer API) · ServiceTrigger `timer` DST · `async` kaskad derinlik · `triggerProcessStep → associatedInstance` ·
> **D3** kalıcılık · `apiKeyId` içeriği · **loglama modeli** · **ortam (env)** · Form List tik & filtreler · adım ayar detayları
> (Flovo AI · `DynamicParameter.value`) · `ActionTransfer.user` · `SchedulerJob` `...At` adlandırma.
>
> **Not:** Login/register (kimlik & erişim) işleyiş dökümantasyonu için yol haritası → ana dizinde [`login-register-plan.md`](../login-register-plan.md)
> (§6 kararları verilince Faz 1'den yazım başlar).

---

## 🆕 Eklenenler
- **Tipe-özel Process Step ayar şemaları** (`models/service-settings/jsonTemplateModels/process-step-settings/`, YENİ) —
  **22 `stepType`** için tip-başına `ProcessStep.settings` (JSONB) ayar dosyası (20 dosya; `timer` ailesi 3 tipi tek dosyada;
  `subProcessStart/End` ayarsız; `triggerProcessStep`/`formRedirect` ertelendi) + index. Her dosya: **§1 settings tablosu**
  (amaç-odaklı açıklamalar) + **§2 alt-modeller** (DynamicParameter · ComparisonCondition[recursive `$ref`] · SendNotification*
  ailesi · Timer*Schedule) + **§3 ortak alanlar** (ProcessStep kolonu) + **JSON Schema** (`additionalProperties:false`, cross-file
  `$ref`) + örnek. 5 paralel ajan; belirsiz alanlar eski koddan/research'ten doğrulandı, açık uçlar todo'ya bağlandı.
  `notification.md`/`timer.md` paylaşılan alt-modelleri kanonik tanımlar; `user`/`userGroup` bunlara cross-ref. **1293 linkten 0 kırık.**

## 🔧 Değişenler / kararlar
- **Adım-özgü modelleme kararı netleşti:** process-step `settings`'te **referans id'ler gömülü** (property'nin çekirdek-kolon FK
  yaklaşımının aksine) → uygulama-katmanı doğrulaması; **profil-bazlı katman yok**; akış yönlendirme `ProcessStepAction`'da.
  `jsonTemplateModels/index.md`'e `process-step-settings/` satırı eklendi.

## 🩹 Düzeltmeler
_(henüz yok)_

## ❓ Yeni açık sorular
- **Process step `settings` — açık alan kararları** (todo.md Tier 3): `comparison` değer-kaynağı (ValueAssignType ↔
  `BusinessRuleConditionCompareValue`/viewProfile) · `comparison` nested grup birleştiricisi · `triggerProcessStep`/`formRedirect`
  şeması hâlâ ertelendi · `flovoAi`/`instanceCreator` detayları.

## ✅ Çözülen açık sorular
_(henüz yok)_

---

*Commit başlığı: **v0.38**.*
