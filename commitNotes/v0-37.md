# v0.37 — Tipe-özel Property ayar şemaları (18 propertyType → `jsonTemplateModels/property-settings/`: 3-katman + JSON Schema) + `dto/` → `jsonTemplateModels/` yeniden adlandırma + `TextAlignment` enum + enum-değer temsili (camelCase string) teyidi

> **Önceki:** [`v0-36.md`](./v0-36.md)
> **Kapsam:** 18 `propertyType` için tip-başına **`Property.settings` (JSONB) ayar şeması** oluşturuldu
> (`models/service-settings/jsonTemplateModels/property-settings/`): **3-katman ayrımı** (settings/çekirdek/profil-bazlı) +
> **amaç-odaklı açıklamalar** (dokümanlardaki atıf yerleri kontrol edilerek) + **JSON Schema** (`additionalProperties:false`).
> `dto/` → **`jsonTemplateModels/`** yeniden adlandırıldı ve `property-settings/` altına alındı; **`TextAlignment`** enum eklendi;
> **enum-değer temsili = camelCase string** teyit edildi (eski int-index yaklaşımı yeni tasarımda kullanılmaz — değişiklik yapılmadı).
> **1113 iç linkten 0 kırık. Bu dosya kapandı; sonraki sürüm `v0-38.md`.**
>
> **Kural:** Açık sorular **yalnız `todo.md`**'de tutulur; diğer dokümanlar buraya işaretçi verir. **Commit-bazında çözülen
> sorular** yalnız ilgili `commitNotes/v0-X.md`'nin "✅ Çözülen açık sorular" bölümünde toplanır (v0.22 kararı).
>
> **Kilometre taşı (v0.36):** Servis + organizasyon ayarı **veri modelleri (30 dosya) gözden geçirildi ve TASLAK'tan
> çıkarıldı** (🟢). Model katmanı geliştirmeye hazır. **Backend için kalan somut ön-koşul:** tipe-özel `settings` **JSON
> Schema'ları** (property 18 tip + process-step 22 tip) — `Property.settings`/`ProcessStep.settings` doğrulama kapısı buna bağlı.
>
> **Bekleyen açık konular (`todo.md`):** **Tipe-özel `settings` JSON Schema'ları** (property + process-step; backend doğrulama
> kapısı) · **Ayar-yazma (design-time CRUD) API yüzeyi** (service/property/step/view-profile) · **İki-katman sınırı** (değer
> atama & karşılaştırma → iş kuralı=frontend UX ↔ adım=kalıcı/akış kararı) · **İş kuralı — kalan detaylar** (JSONLogic **katalog
> operatör imzaları** · `environmentRestriction` değerleri · hata görünürlüğü · cascade **döngü/derinlik limiti** · `search` ·
> `setStyle` `style`-şeması · `showMessage` bir-kez-göster · `httpRequest` alan-yolu JSONPath) · **Motor runtime mimarisi**
> (§2.2 orkestrasyon↔yürütme · §8 kalıcılık/durum yaşam döngüsü · §4.5 paralel dallanma/join) · **Veri modeli** açık kalan:
> **soy ağacı (lineage)** + **çok-kayıt iterasyonu / loop / join** · **Vekalet (proxy) sistemi** (`UserProxy`) · **O3** döngü
> koruması · **O6** dış referans anahtarı `organizationId` vs `organizationCode` (Customer API) · ServiceTrigger **`timer` DST**
> kenar durumları · **`async` kaskad kompozisyonu/derinlik sınırı** · **`triggerProcessStep → associatedInstance`** tetikleme
> tanımı · **D3** satır↔ekstre iş kuralı ↔ alt-süreç kalıcılık · **`apiKeyId`** içeriği (Customer API) · **loglama modeli**
> (`research/settings-log/`; sistem logları) · **ortam (env) / servis template** · Form List tik & filtreler · **adım ayar
> detayları** (Flovo AI · `triggerProcessStep`/`formRedirect` ayarları · `DynamicParameter.value` şekli) · **`ActionTransfer.user`**
> ile creatorUserId opsiyonel atama · **`SchedulerJob` `...At` adlandırma** (altyapı-erteleme).
>
> **Not:** Login/register (kimlik & erişim) işleyiş dökümantasyonu için yol haritası → ana dizinde [`login-register-plan.md`](../login-register-plan.md)
> (§6 kararları verilince Faz 1'den yazım başlar).

---

## 🆕 Eklenenler
- **Tipe-özel Property ayar şemaları** (`models/service-settings/jsonTemplateModels/property-settings/`, YENİ) — **18 propertyType**
  için tip-başına `Property.settings` (JSONB) ayar dosyası + index. Her dosya: **3-katman ayrımı** (settings JSONB · çekirdek kolon ·
  profil-bazlı) + alan tablosu (tip/zorunlu/varsayılan/**amaç-odaklı açıklama** — dokümanlardaki atıf yerleri kontrol edilerek) +
  **JSON Schema** (`additionalProperties:false`) + örnek. 4 paralel ajanla üretildi; belirsiz default'lar eski koddan doğrulandı.
  Backend `settings` doğrulama kapısının + frontend form-üreticisinin doğrudan girdisi. **1113 iç linkten 0 kırık.**
- **`TextAlignment` enum** (`models/enums/text-alignment.md`) — `text` alanının `settings.textAlignment`'ı için (`left`/`center`/`right`); index'e eklendi.

## 🔧 Değişenler / kararlar
- **`models/service-settings/dto/` → `jsonTemplateModels/` yeniden adlandırıldı** (git mv; JSON-only model/şema ailesi: config
  DTO'ları + tip-başına settings şemaları). `property-settings/` bu klasörün altına alındı. 20 dosyadaki yol-atıfları düzeltildi
  (prose'daki `DTO`/`PropertyDto` tip adları korundu); `jsonTemplateModels/index.md` + `service-settings/index.md` güncellendi.

## 🩹 Düzeltmeler
_(henüz yok)_

## ❓ Yeni açık sorular
- **Property `settings` — tip-başına şemalardan açık alan kararları** (todo.md Tier 3): `flowInfoValue`/`userInfoValue` enum'a
  çekilsin mi · Form List red-akışı bayrakları (`isRejectReasonRequired`/`isReapprovalLockedAfterReject`) yerleştirilmedi ·
  `groupByTaxReceipt` eski-kod ayar adayları kapsam-dışı.

## ✅ Çözülen açık sorular
- **Enum-değer temsili = camelCase string (TEYİT, v0.37):** yeni tasarımda enum'lar JSONB'de **string** olarak taşınır
  (eski Flutter'ın **int-index** serileştirmesi kullanılmaz) — self-describing + yeniden-sıralama-güvenli + JSON Schema yerel +
  çok-client portlanabilir. Kullanıcı onayıyla mevcut konvansiyon korundu; değişiklik yapılmadı.

---

*Commit başlığı: **v0.37**.*
